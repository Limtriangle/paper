# INBOX.md — human <-> agents

Agents append questions under **Open questions** (`- [ ]`). The author answers by editing
the line to `- [x] ... → <answer>`; master reads it on the next sweep and logs the answer
in DECISIONS.md with `[human]`. Agents never wait for an answer; they apply the plan's
default until one arrives. **Exception: Gate 0 (the topic) waits for the author.**

## Open questions
- [ ] (Gate 0) **Topic.** Candidates in `ralph/phase0_candidates.md` (v4). **Master recommends C1 as rewritten from your red-team R2**: the nested HVI ladder inside one CIDNet — A0 HVI learned-k → A2 k = 0 (Cartesian HSV) → A3 max + linear (Cb,Cr) → A4 YCbCr, plus L1 (no auxiliary HVI loss) — same frozen loss in every arm (L_rgb + L_hvi at constant detached k0, no VGG on HVI), **5 matched seeds**, primary read-out = final epoch-1000 checkpoint, primary metric = GT-mean PSNR with one scalar luminance gain, paired-by-seed t-tests with Holm, TOST ± 0.3 dB for equivalence, mechanism rule on the darkest deciles + darkness dose–response, pipeline gate on A0 (raw PSNR ∈ [23.3, 24.3], SD ≤ 0.5), pivot to C3 on failure. Selection bias (old C2) comes free from a post-hoc oracle pass. **25 core runs ≈ 2.0 days on 4 GPUs** (≈ 2.7 with the optional A1/R/U arms). One deviation from R2, for the integrity floor: the test "oracle" is computed once, post hoc, from checkpoints saved every 10 epochs, not during training. Answer with `[human] topic confirmed: C1` (or another #, or your own question). Until then: instrument work only (scene-disjoint val split, detached k0 loss, checkpoint cadence).

## Answers / notes from the author
- 2026-09-23 ~19:05 UTC [human-pane] Author note typed into the master pane: red-team results in ralph/related/redteam/ (R1, R2); rewrite C1 as R2's nested ladder and re-post the recommendation; no runs; Gate 0 still open. → done (candidates v4, this INBOX entry). If this note was not yours, say so here and I will revert.
- 2026-09-24 [human] The earlier studies are discarded. Start the topic from scratch; it must be related to HVI-CIDNet.
- 2026-09-24 [human] Top-tier literature pass done: `ralph/related/SURVEY.md` (narrative + gaps), `INDEX.md` (counts at its foot), `survey.bib`, slices A–F. writing: merge into custom.bib only after your own bibtex-verify pass; fill phase0_related.md §5 from it. master: SURVEY.md §1 maps each gap to C1–C6.
- 2026-09-24 [human] Red-team of C1 landed: `ralph/related/redteam/R1_prior_controlled_comparisons.md` (verdict: PARTIALLY DONE, not redundant — authors' own single-run ablations disagree across datasets; no seeds; no YCbCr row) and `R2_design_redteam.md` (verdict: C1 as worded is confounded and underpowered; use the nested single-factor ladder A0→A2→A3→A4 + L1 with a frozen loss, 5 matched seeds, final-checkpoint primary read-out, per-intensity-bin mechanism analysis, pre-registered decision rules). master: rewrite C1 in phase0_candidates.md as this ladder (it absorbs C2 and the k question from C3) before the author decides Gate 0. Topic is NOT yet confirmed.
