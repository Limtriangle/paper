# INBOX.md — human <-> agents

Agents append questions under **Open questions** (`- [ ]`). The author answers by editing
the line to `- [x] ... → <answer>`; master reads it on the next sweep and logs the answer
in DECISIONS.md with `[human]`. Agents never wait for an answer; they apply the plan's
default until one arrives. **Exception: Gate 0 (the topic) waits for the author.**

## Open questions
- [ ] (Gate 0, **new criteria**) **Method.** Candidates M1–M5 in `ralph/phase0_candidates.md` Part I (**v7**, your novelty checks folded in, your numbering: M1 exposure-conditioned, M2 adaptive density, M3 chroma confidence). All three are incremental per your checks; ranked by novelty margin × loop fit: **master recommends M2 — per-pixel, noise-calibrated density k(x) with a per-pixel conditioning bound C_{k(x)}(I) ≥ c·σ(I)/I (extends BC-IHV's global κ to a field; Wiener-like chroma shrinkage inside an invertible encoding), with M3's confidence-weighted hue loss and the dark-decile hue/ΔE00 protocol folded in, M4's loss fix as wave 1.** Go/no-go from your check: beat RHVI-style I-refinement AND the ISP chroma-suppression LUT by ≥ 0.5 ΔE00 or ≥ 2° hue in deciles 1–2 at equal global PSNR. **Pivot: M1** (exposure-conditioned HVI parameters; go/no-go ≥ 1 dB at the worst brightness level over gamma-aug and the normalise/invert wrapper). M3 kept as a zero-run comparison against CMIG-Net/TCA-Net outputs. C1 ladder = ablation chapter. Answer with `[human] topic confirmed: M2` (or M1/M3/…, or your own component).

- [ ] (Protocol, default applied) LOL-v1 test overlaps train: 14/15 eval15 images have a train near-duplicate (cos > 0.9); 24 train images share a scene with the test set. **Default: keep them in training** (comparability with upstream; matched across arms) and report the caveat + a deduplicated LOL-v2-Real check. Say `[human] drop overlapping train images` to flip it before launch.

## Answers / notes from the author
- 2026-09-24 ~03:30 UTC [human-pane] "Novelty checks for M1-M3 are in ralph/related/novelty/. Fold them into candidates v5." → done as v7 (INBOX line 19 confirms).
- 2026-09-23 ~19:05 UTC [human-pane] Author note typed into the master pane: red-team results in ralph/related/redteam/ (R1, R2); rewrite C1 as R2's nested ladder and re-post the recommendation; no runs; Gate 0 still open. → done (candidates v4, this INBOX entry). If this note was not yours, say so here and I will revert.
- 2026-09-24 [human] The earlier studies are discarded. Start the topic from scratch; it must be related to HVI-CIDNet.
- 2026-09-24 [human] Top-tier literature pass done: `ralph/related/SURVEY.md` (narrative + gaps), `INDEX.md` (counts at its foot), `survey.bib`, slices A–F. writing: merge into custom.bib only after your own bibtex-verify pass; fill phase0_related.md §5 from it. master: SURVEY.md §1 maps each gap to C1–C6.
- 2026-09-24 [human] Red-team of C1 landed: `ralph/related/redteam/R1_prior_controlled_comparisons.md` (verdict: PARTIALLY DONE, not redundant — authors' own single-run ablations disagree across datasets; no seeds; no YCbCr row) and `R2_design_redteam.md` (verdict: C1 as worded is confounded and underpowered; use the nested single-factor ladder A0→A2→A3→A4 + L1 with a frozen loss, 5 matched seeds, final-checkpoint primary read-out, per-intensity-bin mechanism analysis, pre-registered decision rules). master: rewrite C1 in phase0_candidates.md as this ladder (it absorbs C2 and the k question from C3) before the author decides Gate 0. Topic is NOT yet confirmed.
- 2026-09-24 [human] NEW CRITERIA for Gate 0 (supersedes the C1 ladder as the primary direction): the thesis must (a) propose a NEW METHOD component built on HVI-CIDNet, (b) show it improves performance at a SPECIFIC, measurable locus (e.g. dark low-saturation pixels, brightness-shifted inputs, cross-dataset), and (c) support FAST iteration: fine-tune from the released LOL-v1 weights for ~50 epochs (≈25 min/run on one 2080 Ti, 4 in parallel), validation-split metric per iteration, test once at the end, then 3 seeds from scratch only for the final table. The C1 ladder becomes the ablation chapter of whatever method wins, not the thesis. master: produce method candidates M1–M5 in ralph/phase0_candidates.md v5, each with: the failure locus + evidence from ralph/related/ (MILL brittleness, NTIRE 2026 zero-shot 13.85 dB, near-black ill-conditioning, VGG-on-HVI oddity, LCA asymmetry), the proposed component, the fast-iteration loop, the baselines that must be beaten (incl. upstream random-gamma augmentation, BC-IHV, RHVI-FDD, TCA-Net, VCR, PAL, GT-mean loss), and a one-line novelty risk. experiment: set up the fine-tune loop (released w_perc weights, 50-epoch schedule, scene-disjoint val) and verify its ranking agrees with a full run on one pair before anyone iterates. No runs beyond that instrument check. Topic still NOT confirmed.
- 2026-09-24 [human] Novelty checks for method candidates M1/M2/M3 are in ralph/related/novelty/ (all INCREMENTAL; each file names the closest work, the baseline to beat, and the narrowly novel version). master: use them in candidates v5.
- 2026-09-24 [human] Six transfer-style candidates T1-T6 checked (ralph/related/novelty/T*.md). NOVEL-IN-HVI: T1 hue-rotation-equivariant HV branch, T3 test-time adaptation of k/gain/gamma, T4 learned loss weights, T6 HVI-space distillation (thin). INCREMENTAL: T2, T5. Author leaning T1 primary (+T3 as extension). master: add T1-T6 to candidates v5 with the fast fine-tune loop; still no runs.
