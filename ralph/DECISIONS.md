# DECISIONS.md — every decision, one line, with the why

Format: `<ISO time> | <who> | <decision> | <why> | <evidence path>`
Gate verdicts: `GATE <name>: PASS|FAIL|PIVOT-x`. Human answers carry `[human]`.

2026-09-24T17:00 | scaffold | Adopt writing-driven-autoresearch harness; ICML template, English; repo github.com/Limtriangle/paper; herdr 3-pane topology | author's choice | CLAUDE.md
2026-09-24T17:00 | scaffold | Repo lives in /home/work/research/paper with /home/work/paper as symlink | /home/work is wiped on session end; research/ is the persistent vfolder | bootstrap.sh
2026-09-24T17:00 | scaffold | verify-phm.py is spec-driven (ralph/phm-spec.json), not hardcoded | upstream hardcoded Depth-AR keys; a spec keeps rule 6 (verify by key) without editing the tool per claim | tooling/verify-phm.py
2026-09-24T18:00 | [human] | Discard the earlier studies (loss ablation / width / prefilter) and their exported results; choose the thesis topic from scratch, HVI-CIDNet related | author's decision | HVI-PLAN.md §Phase 0
2026-09-24T18:00 | scaffold | Gate 0 (topic) is the one gate the author calls; team does instrument work only until then | the topic is the author's, and idle GPUs must not tempt the team into an unplanned study | HVI-PLAN.md §5
2026-09-23T18:00 | master | herdr topology installed from herdr/topology.json; permission mode live; watcher under Monitor | workers hang on prompts without it | herdr/permission-policy.md
2026-09-23T18:02 | master | Approved writing's read-only `cat`/`ls` over writing/ + ralph/ (classifier false positive on shell expansion) | policy: auto-approve reads inside project dirs | herdr/permission-policy.md
2026-09-23T18:05 | master | Phase 0 dispatch: experiment → phase0_codebase.json + 1-epoch LOLv1 smoke on GPU 0 + export ingest + cost.smoke; writing → phase0_related.md + verified custom.bib; master → candidates after both land | plan §Phase 0 steps 1–3; instrument work only until Gate 0 | HVI-PLAN.md §Phase 0
2026-09-23T18:12 | master | Approved writing's WebFetch of arxiv.org (bib verification); will approve DBLP/Semantic Scholar/arXiv reads on sight, plain Yes only | citation verification is required by integrity floor rule 5; a fetch is a read, not an install | herdr/permission-policy.md
2026-09-23T18:40 | master | phase0_candidates.md v1 written (C1–C6, ranked); recommendation C1 (+C2, +C5, pivot C3) posted to INBOX | plan §Phase 0 steps 2–3; drafted before phase0_codebase.json to save wall-clock, hours to be reconciled when cost.smoke lands | ralph/phase0_candidates.md
2026-09-23T18:45 | experiment | PROPOSAL: validation = 50 images held out from our485 (split seed 42, sha 3b792413…), eval15 = test, guarded in code (hvilib.open_rgb refuses eval15 unless final_eval_test.py) | upstream has no LOLv1 validation split and monitors checkpoints on eval15, the paper's test set; integrity floor #4 | auto-research/hvilib.py, ralph/results/phase0_codebase.json
2026-09-23T18:45 | experiment | PROPOSAL: checkpoint selection by ungated validation PSNR (uint8, no GT mean); gated (x1.3 saturation, upstream eval.py) and GT-mean variants recorded alongside, never used for selection | the gain is a fixed test-time post-process, not the network; both are reported so master can pick the reporting convention at Gate 0 | auto-research/hvilib.py validate_psnr/detailed_evaluation
2026-09-23T18:45 | experiment | Smoke = first 2 epochs of the full 1000-epoch schedule (not 1) | simulated upstream schedule: epoch 1 trains at lr=0 (warmup starts at 0); epoch 2 proves the update path | ralph/results/phase0_codebase.json instrument.schedule
2026-09-23T18:45 | experiment | Instrument deviations from upstream, all logged in the manifest: deterministic seed + cudnn.deterministic + TF32 off; validation every epoch; no grad clip (upstream's is a no-op); 3 loader workers per job | reproducibility and 4-job sharing of 15 cores; none changes the objective or the data | phase0_codebase.json instrument.deviations_from_upstream
2026-09-23T18:45 | experiment | Cost measured: upstream protocol on one 2080 Ti = 26.5 s/epoch train + 6.5 s val, peak 9.75 GiB allocated (10.5 GiB reserved of 11) → ~9.1 h per 1000-epoch run; batch 8 crop 256 FP32 fits with <1 GiB headroom | budget input for Gate 0 candidates: ~4 full runs per 9 h on 4 GPUs | phase0_codebase.json cost
2026-09-23T18:45 | experiment | Always export with --run <study>/<run>; a bare `export_results.py` would re-export the discarded pre-reset studies still under outputs/ (loss_study, width_study, prefilter_study, loss_ablation); run dirs are never moved or deleted by experiment | persona §6; master decides whether to archive them | tooling/export_results.py
2026-09-23T19:05 | experiment | Rule-9 audit PASS in a scratch copy (PAPER/RESEARCH env overrides, no paper or run touched): verify-phm.py flags MISMATCH (+1.0 planted), MISSING KEY, MISSING FILE, UNBACKED \phm and exits 1; export_results.py reproduces a +5 dB tampered validation_summary.csv verbatim (never corrects) and its sources sha256 changes, after which the previously-correct spec entry fails as MISMATCH | master's request in STATUS.md; the tools do what the integrity floor needs | scratchpad rule9/ (not kept)
2026-09-23T19:05 | master | Auto-approve (plain Yes) web fetches to citation domains (arxiv, dblp, semanticscholar, doi, openreview, github, publisher sites) via a master-run poller; all other prompts still judged by hand | writing's bib verification issues dozens of read-only fetches and each round trip through master stalled it 20 min | herdr/permission-policy.md (reads inside scope; fetch = read)
2026-09-23T19:30 | experiment | FACT: upstream I_LCA5 is dead code — its output is overwritten before use (net/CIDNet.py:105,109); 70,960 of 1,975,569 parameters never receive a gradient; the I branch effectively has 5 LCA blocks | measured: p.grad is None for every I_LCA5.* tensor after backward | ralph/results/phase0_codebase.json facts.architecture.dead_block
2026-09-23T19:30 | experiment | FACT: default CUDA training is not bitwise reproducible — 3 same-seed replicates share identical init and an identical lr=0 epoch 1, then diverge (val PSNR spread 0.042 dB after epoch 2); strict mode (torch.use_deterministic_algorithms) is bitwise exact at 1.5x train time (40 vs 26 s/epoch; 13.6 h vs 9.1 h per 1000-epoch run) | instrument now supports --strict as a protocol field; master picks at Gate 0 | phase0_codebase.json reproducibility, cost_strict
2026-09-23T19:30 | experiment | PROPOSAL (default): run studies in default mode and treat same-seed run-to-run noise as a measured floor under the seed spread; use --strict only if a claim needs bitwise replication | 50% more GPU time buys reproducibility of a number that is itself below the seed SD; the simpler option per persona §7 | phase0_codebase.json
2026-09-23T19:30 | experiment | Instrument options added for the candidate studies: --snapshot-every N (upstream snapshots=10; needed by C2's checkpoint audit), final_eval_test.py (the only eval15 reader; --preflight green on validation images; refuses a second read per label) | readiness for Gate 0; no study launched | auto-research/README.md
2026-09-23T19:20 | master | Approved writing's python heredoc that downloads arXiv HTML full texts and converts them to .txt scratch files | read from a citation domain + scratch output, nothing destructive; needed for phase0_related.md | herdr/permission-policy.md
2026-09-23T19:45 | master | ACCEPT experiment's proposal: studies run in default (non-strict) CUDA mode; same-seed run-to-run noise is exported as a measured floor and reported under the seed spread; --strict only if a claim needs bitwise replication | 1.5x GPU cost buys reproducibility of a quantity (0.04 dB) already below the seed SD; persona §5 "simpler option" | ralph/results/phase0_codebase.json reproducibility, cost_strict
2026-09-23T19:45 | master | experiment used GPUs 2-3 for two same-seed replicates beyond the "one GPU" dispatch; accepted retroactively as instrument work (minutes, no study, no test read) | cheap, informative, reversible; the boundary that matters is "no study before Gate 0" and it held | ralph/RESULTS.md
2026-09-23T19:45 | master | phase0_candidates.md v2: hours reconciled to R≈9.1 h/run (7.7 h if val every 5 epochs); ranking unchanged; C1 ≈ 1.6 days on 4 GPUs | measured cost.projected_hours_full_schedule | ralph/results/phase0_codebase.json
2026-09-23T19:45 | master | Gate A verdict deferred to Gate 0 although its inputs exist | the protocol hash to pin depends on the topic (adapters, val cadence, strict flag); pinning now would be re-done | HVI-PLAN.md §5
2026-09-23T20:05 | master | Corrected master notes + candidates after cross-check vs phase0_codebase.json: perceptual range oddity is RGB-side (range_norm), upstream code monitors on eval15 but does not auto-select; paper protocol (1500 ep, 400 crop) ≠ code (1000, 256) | notes were subagent reads, JSON is measured | ralph/results/phase0_codebase.json
2026-09-23T20:15 | master | ACCEPT experiment's 18:45 proposal: validation = 50 images held out from our485 (seeded shuffle 42, sha256-pinned), 435 train; eval15 = test, opened only by final_eval_test.py | upstream has no LOLv1 val split and monitors on the test set; integrity floor rule 4 | auto-research/hvilib.py, ralph/results/phase0_codebase.json split.sha256
2026-09-23T20:15 | master | ACCEPT experiment's 18:45 proposal: checkpoint selection by ungated validation PSNR (no GT-mean). Reporting convention pre-committed for Gate 0: PRIMARY = ungated, no GT-mean (the raw network output); gated (×1.3 saturation) and GT-mean recorded as secondary columns and labelled as test-time post-processing | the gate and GT-mean are post-processes, not the network; the thesis question is about the color space | auto-research/hvilib.py
2026-09-23T18:40+00:00 | [human] | Literature survey (6 slices, 122 entries) added under ralph/related/; gaps 1,3,4 support C1+C2, gap 2 supports C3 | evidence for Gate 0 | ralph/related/SURVEY.md
2026-09-23T18:41 | master | CORRECTION: master's DECISIONS timestamps written today as 19:05–20:15 were estimates; the real wall clock for those entries was ≈18:05–18:40 UTC (ordering is correct). experiment's 19:30 entries were likewise written ≈18:24. From here on every entry uses `date -u` | audit trail must not mislead about when | this line
2026-09-23T18:41 | master | experiment's pane held unsent input text "Gate 0: topic confirmed C1, write the study scripts" — NOT from INBOX, NOT from master; treated as a Claude Code prompt suggestion, cleared with Escape. Gate 0 remains OPEN; the only valid confirmation is a [human] line in ralph/INBOX.md | integrity: a suggestion is not a decision | herdr read experiment
2026-09-23T18:43 | master | FIX herdr/herdr_sync.py send: 'herdr agent send' does not exist in herdr 0.9.1, replaced with 'herdr pane send-text'; no text dispatch reached any worker before this (workers ran on their personas); approvals were unaffected (send-keys) | comms tool must work; verified by ping | herdr/herdr_sync.py:605
2026-09-23T18:46 | master | ACCEPT experiment's colour-space adapters (5 conditions, lossless round-trip, black->black exact) and the eval15 listing guard; commit 0f0ed67 | pre-registration for C1; instrument for Gate A is complete pending the protocol hash at Gate 0 | ralph/results/phase0_colorspace_roundtrip.json
2026-09-23T18:46 | master | FACT correction to master notes: measured inverse gain per unit chroma at I<1/255 is hvi 0.014, hvi_nock 0.005, srgb 0.02, hsv 0.02, ycbcr 1.25 — the HVI inverse does NOT amplify chroma error near black in float32; the ÷C_k blow-up exists only at I≡0. C3's near-black premise is narrowed to the exact-black case and hue indeterminacy | measured beats a code-reading estimate | ralph/results/phase0_colorspace_roundtrip.json adapters.*.near_black_I_below_1_over_255
2026-09-23T20:30 | writing | Phase 0 deliverables landed: ralph/phase0_related.md (claims, every ablation row, open gaps §4.2) and custom.bib with 28 entries each verified against a fetched Crossref/CVF/arXiv page; skeleton still green | HVI-PLAN.md §Phase 0 step 1 | ralph/phase0_related.md, writing/icml2024/custom.bib
2026-09-23T20:30 | writing (proposal for master/experiment) | Two facts from the upstream code that the frozen protocol must decide on: (a) upstream "validation" for lol_v1 is eval15, i.e. the test split, so checkpoint selection there violates floor rule 4 and a validation subset must be carved from our485; (b) clip_grad_norm_ runs before zero_grad/backward (train.py 66-71) so clipping is a no-op; keep faithful or fix, but state which | phase0_related.md §1.4 | /home/work/research/code/HVI-CIDNet/train.py
2026-09-23T18:52 | master | ACCEPT writing's Phase 0 deliverables: ralph/phase0_related.md + writing/icml2024/custom.bib (28 entries, each verified against DBLP/Crossref/OpenAlex/arXiv); build + audit green; commit 2042e4f | plan §Phase 0 step 1 complete for both workers | ralph/phase0_related.md
2026-09-23T18:52 | master | RULING on writing's flag "upstream selects on eval15": already handled — our protocol selects on a 50-image val split carved from our485; eval15 read once by final_eval_test.py | integrity floor rule 4 | DECISIONS 20:15 entry (real ≈18:40)
2026-09-23T18:52 | master | RULING on writing's flag "grad clip is a no-op": the frozen protocol keeps upstream's ACTUAL behaviour (no effective clipping) identically across all conditions; the paper states this as a documented property of the reproduced code, not silently "fixed" | a controlled comparison needs identical training, not the paper's stated intent; changing it would make our baseline a different method | ralph/results/phase0_codebase.json facts.training
2026-09-23T18:53 | master | phase0_candidates.md v3 after cross-review vs phase0_related.md: the paper's colour-space ablation used a proxy net (HVI 21.56 vs HSV 21.35 vs sRGB 20.06, LOL-v2-Real, single run), not CIDNet; 24.11 and best_PSNR/best_SSIM belong to LOL-v2-Real; C1 motivation rewritten around the 0.2 dB margin; CIEDE2000 added to C1 metrics; k sweep stated in code convention; baseline protocol = the code (1000 ep, 256 crop), not the paper text (1500 ep, 400 crop) | premises must match the sources; ranking unchanged, C3 noted as close second | ralph/phase0_related.md §3, gap 13
2026-09-23T19:00+00:00 | [human] | C1 must be redesigned as a nested single-factor ladder (R2) before Gate 0; not redundant per R1 | red-team findings | ralph/related/redteam/
2026-09-23T19:03 | [human-pane] | Author note in master pane: red-team R1/R2 in ralph/related/redteam/; rewrite C1 as R2's nested ladder (A0/A2/A3/A4 + L1, frozen loss, 5 matched seeds, decision rules), re-post recommendation; no runs; Gate 0 open | author's instruction, conservative and reversible; acted on and mirrored in INBOX for confirmation | ralph/related/redteam/R2_design_redteam.md
2026-09-23T19:03 | master | phase0_candidates.md v4: C1 = R2 ladder; C2 folded into C1 read-outs; ranking unchanged; cost 25 core runs ≈ 190 GPU-h ≈ 2.0 days (measured 7.7 h/run with val every 5 epochs) | R1 (partially done, not redundant) + R2 (confounds, power, protocol) | ralph/phase0_candidates.md
2026-09-23T19:03 | master | RULING CHANGE (supersedes the 18:40 reporting ruling): primary metric = GT-mean PSNR with one scalar luminance gain (never per-channel); primary read-out = final epoch-1000 checkpoint; val-selected secondary; raw PSNR/SSIM/LPIPS/ΔE00/log-exposure secondary; gated/alpha/gamma knobs fixed to identity in all arms | R2 §4b–4c, adopted at the author's request | ralph/related/redteam/R2_design_redteam.md
2026-09-23T19:03 | master | RULING: R2's test oracle (test PSNR every 10 epochs) is NOT computed during training; checkpoints are saved every 10 epochs and the oracle is computed post hoc, once per arm, by the frozen final_eval_test.py in a dedicated labelled pass after all arms finish; never used for selection or ranking | keeps integrity floor rule 4 literally while delivering C2 | HVI-PLAN.md §1, CLAUDE.md rule 4
2026-09-23T19:03 | master | RULING: val split becomes scene-disjoint (embedding + pHash clustering of all 500 LOL-v1 images, ≈40 val pairs from clusters with no test image); supersedes the random 50-image split for C1; instrument work for experiment now, no training | R2 §4a: random val shares scenes with train/test and rewards memorisation | ralph/related/redteam/R2_design_redteam.md
2026-09-23T21:05 | writing | phase0_related.md corrected per master: (a) perceptual range_norm oddity is RGB-side ([0,1]->[0.5,1]), HVI lands in [0,1]; (b) every uncited claim now carries a verified key, "bib key pending" (Bread/GLARE/FourLLIE/FusionNet, verification in flight) or "unverified"; (c) §3 notes the old paper's "k sits in the inverse" contradicts the code (this_k is a detached float) | master's dispatch in STATUS.md | ralph/phase0_related.md
2026-09-23T21:05 | writing (proposal) | C1 table scaffold: writing/tables/spec.json -> c1_colorspace.tex (ungated, PSNR/SSIM/LPIPS/CIEDE2000, mean + SD over 3 seeds) and c1_colorspace_gated.tex (gated + GT-mean). Contract for experiment: c1__colorspace_v1.json must carry analysis.analysis_c1.decision.summary.<cond>.<metric>.{mean,sd,n}, cond in {srgb,hsv,ycbcr,hvi_nock,hvi}. Neither table is \input until the src exists | a scaffold that names its keys before the run is the point of writing-first | writing/tables/spec.json
2026-09-23T21:05 | writing (tooling, for master to veto) | gen-table.py: a missing src now prints MISSING and renders "--" instead of crashing; absolute keys may contain {cond}, replaced by the row's job. No change to how existing keys are read or bolded | master asked for "reports missing instead of crashing" | tooling/gen-table.py
2026-09-23T19:04 | master | Claude Code session usage at 96% (resets 22:40 UTC) — all three agents share it. Workers told to commit early and hold; no new subagent fan-out until reset. Nothing on GPU is affected (nothing runs) | avoid losing uncommitted work when the limit hits | writing pane banner
2026-09-23T21:20 | writing | Comparator citation batch (Bread, PairLIE, QuadPrior, FourLLIE, GLARE, DiffLight, CoLIE, Zero-IG, LightenDiffusion, FusionNet, MIRNet, Smith 1978) stopped unverified at master's request (session limit 96%, reset 22:40 UTC); all four mentions in phase0_related.md now read "unverified, no bib entry"; custom.bib stays at 28 verified entries. Re-run the batch after the reset via Crossref/CVF/arXiv (not DBLP/S2) | master's hold order | ralph/phase0_related.md
2026-09-23T19:05 | master | ACCEPT writing's corrections (range_norm, citations, k-in-inverse), C1 table scaffold (c1_colorspace, c1_colorspace_gated; not \input yet) and gen-table missing-src handling (2991e67) | Phase 0 instrument work; scaffold renders '--' cells | writing/tables/spec.json
2026-09-23T19:05 | master | REQUIREMENT (rule 9 spot-check): gen-table.py --check exits 0 when a src is MISSING; that must become nonzero unless --allow-missing is passed, and writing-audit.sh --final must fail on any MISSING src or any '--' cell in an \input'd table. Owner: writing after the session reset; master re-verifies with a planted missing src | an all-dash table must never pass the final audit | tooling/gen-table.py load_src
2026-09-23T19:10 | experiment | Scene-disjoint val split scene_v1 written (40 val / 445 train, sha256 b0dde627…): DINOv2-small cos>=0.85 or pHash hamming<=10, connected components (217 clusters over all 500 GT images), whole clusters with no eval15 member, seed 42 | R2 §4a; per master dispatch | auto-research/split_scene_v1.json, ralph/results/phase0_val_split_scene.json
2026-09-23T19:10 | experiment | FACT/CAVEAT: 14 of 15 eval15 images have a train near-duplicate at DINOv2 cosine > 0.9 (13 at > 0.95); 24 train images share a scene cluster with eval15 — LOL-v1 test scenes are in the training set; belongs in the thesis limitations | measured by scene_split.py, which listed+embedded eval15 GT under a recorded permit; nothing evaluated | ralph/results/phase0_val_split_scene.json eval15_nearest_train
2026-09-23T19:08 | master | ACCEPT experiment's scene-disjoint split (6965222): 40 val / 445 train, sha b0dde627, val clusters contain no eval15 image | R2 §4a | ralph/results/phase0_val_split_scene.json
2026-09-23T19:08 | master | FACT: 14/15 eval15 (LOL-v1 test) images have a train near-duplicate at DINOv2 cosine > 0.9 (13 at > 0.95); 24 train images share a scene cluster with eval15 | measured by experiment | ralph/results/phase0_val_split_scene.json
2026-09-23T19:08 | master | RULING (default, reversible at Gate 0): the 24 overlapping train images STAY in training for every arm; the overlap is reported as a stated caveat of LOL-v1; the deduplicated LOL-v2-Real evaluation becomes the mandatory generalisation check instead of an optional one | removing them would break comparability with upstream and the pipeline gate's [23.3, 24.3] window; matched across arms so no contrast is biased; the author can flip this with one line in INBOX | R2 §4a, §5.3
2026-09-23T22:43 | [human] | INBOX: top-tier literature survey landed in ralph/related/ (SURVEY.md, INDEX.md, survey.bib, slices A–F); writing merges survey.bib into custom.bib only after its own verify pass and fills phase0_related.md §5; master uses SURVEY.md §1 gap→candidate map | author's instruction | ralph/INBOX.md
2026-09-23T22:43 | [human] | INBOX confirms the red-team note was the author's: rewrite C1 as the R2 ladder before Gate 0; topic NOT yet confirmed | already done in candidates v4 (bb85961) | ralph/INBOX.md
2026-09-23T22:43 | master | Resumed after the session reset: watchers re-armed; experiment → arm wiring + preflight; writing → --check must fail on missing src, survey merge after verification, remaining bib checks | STATUS.md Next actions | ralph/STATUS.md
2026-09-23T22:45 | master | candidates v5: reconciled with SURVEY.md §1 — A0−A2 is a seeded replication of HVI-CIDNet+'s C_k on/off (27.99 vs 28.85, one run); TPCNet ran YCbCr vs HVI in its own net (YCbCr ahead, one run); YCbCr never inside CIDNet; k never swept (strengthens C3 as pivot); GT-mean caveat cited (Liao ICCV 2025); survey keys added to C1/C4/C5 | author's instruction to use the gap map | ralph/related/SURVEY.md
2026-09-23T23:05 | writing | FACT: gen-table.py --check now exits 1 on a MISSING src unless --allow-missing (planted: scaffold spec with absent c1__colorspace_v1.json -> exit 1 without flag, exit 0 with flag); writing-audit.sh runs --check with --allow-missing before --final and without it at --final, and a new check 9d fails --final on any "--" cell in an \input'd table (planted \input{tables/c1_colorspace} in experiments.tex -> "5 empty (--) cell(s)" FAIL at --final, warn before; file restored). Non-final audit PASS with warn on the scaffold | master's requirement in STATUS.md | tooling/gen-table.py, tooling/writing-audit.sh
2026-09-23T22:50 | experiment | C1 arm wiring done, no runs: auto-research/c1_arms.py (ArmNet = CIDNet body transcribed from net/CIDNet.py:74-118 between encode/decode; A0 wrapper == upstream forward bitwise), FrozenLoss (L_rgb + L_hvi at constant k0=0.2 in a separate RGB_HVI with requires_grad False, no VGG on HVI), arms A0/A2/A3/A4/L1/R/U; ladder parameter count 1,975,569 identical, R = +324 (36->3 output conv); transforms FP32 under autocast-off; test-time knobs identity; val every 5, snapshots every 10, split scene_v1 by default | master dispatch after reset; R2 §Revised design | ralph/results/phase0_arms_preflight.json
2026-09-23T22:50 | experiment | Preflight facts: model k receives gradient only in HVI-representation arms (A0 -2.19, L1 -0.69, U -5.66 on the synthetic step; U's extra share comes through the loss transform, the coupled-k path R2 §1b warns about); in A3/A4/R k is an unused parameter (counted, no gradient) | recorded per arm | phase0_arms_preflight.json arms.*.model_k
2026-09-23T22:50 | experiment | tooling/export_results.py: best-epoch lookup now ignores rows without a numeric val_psnr (validation every 5 epochs leaves blanks) | needed by --val-every; one-line change, no output-format change | tooling/export_results.py
2026-09-23T22:47 | master | ACCEPT experiment's arm wiring (d124e95): 7 arms CPU fwd/bwd green; A0/A2/A3/A4/L1/U = 1,975,569 params identical, R = +324 (3-ch residual head, off-axis by design); A0 == upstream forward bitwise; loss-side k0 no gradient; FP32 transforms; knobs identity; val every 5, snapshots every 10, split scene_v1. Gate A inputs complete; verdict stamped with the protocol hash at Gate 0 | R2 revised design fully instrumented without a single training run | ralph/results/phase0_arms_preflight.json
2026-09-23T23:20 | experiment | Pre-registration instruments written, CPU-only, no runs: final_eval_test.py (final + --oracle modes, per-image deciles + stress test via c1_metrics.py; both preflighted on validation images; eval15 untouched) and analysis_c1.py (rules 1-7 of candidates v5 C1); GT-mean = ONE scalar BT.601 luminance gain, never per-channel | master dispatch | ralph/results/phase0_analysis_selftest.json, auto-research/README.md
2026-09-23T23:20 | experiment | Stress-test noise model fixed now (frozen with the script): x' = Poisson(f·x·500)/500 + N(0, 0.01), f ∈ {1 (no noise), 0.5, 0.25}, seed = crc32(image:f) so every arm sees identical noise | R2 §5.4 leaves the noise unspecified; a documented constant is needed before launch | auto-research/c1_metrics.py
2026-09-23T23:20 | experiment | Analysis self-test: planted +0.6 dB → 'difference' (all four rule-3 conditions), planted nulls → 'equivalent' (TOST), planted +0.15 dB → 'equivalent' (below SESOI and inside the margin at the synthetic noise level); decile rule 5 and stress monotonicity detected as planted; one bug found and fixed (statsmodels vcomp is an array) | recorded both outcomes as required | ralph/results/phase0_analysis_selftest.json
2026-09-23T22:54 | master | ACCEPT experiment's pre-registration instruments (f330fb2): final_eval_test.py final + --oracle (preflighted on val only), c1_metrics.py (scalar-gain GT-mean, deciles, hue error, frozen stress noise), analysis_c1.py rules 1–7; self-test: planted +0.6 dB → difference, nulls → equivalent, +0.15 dB → equivalent (below SESOI), decile rule and stress monotonicity detected; sha256 recorded | rule 9 (instrument shown able to fail/pass) satisfied before any run | ralph/results/phase0_analysis_selftest.json
2026-09-23T23:40 | writing | Survey merged: all 103 entries of ralph/related/survey.bib verified by five subagents (one fetched page each; Crossref/CVF/arXiv/OpenAlex/ACL/JMLR/MLSys/NeurIPS/IJCAI), 0 rejected; plus wang2023fourllie, feng2024difflight, smith1978hsv. custom.bib now 112 entries, no duplicate keys, scratch build with \nocite{*} gives 0 BibTeX warnings. Master's three issues: yan2025hvicidnetplus is IEEE TCSVT 36(8):12126-12139, 2026 (two independent Crossref fetches) with author list Shi, Ma, Feng, Hu, Wu, Pang, Yan, cited as the journal version; pilligua2025multiintensity merged into pilligua2025mill; yan2025hvi (CVPR 2025) kept from custom.bib. Every key candidates v5 cites survives unrenamed: shi2025tpcnet (now ECCV 2026, changed title), cheng2026vcr, yang2026rhvifdd (arXiv-only), bouthillier2021variance, musgrave2020reality, liao2025gtmean, shi2025fusionnet, pilligua2025mill. Other corrections: guo2023bread author order (Guo, Hu per publisher), Retinexformer 2nd author Hao Bian, InterLight = IJCAI 2026, Multinex = CVPR 2026, cheng2026lhsi author order per WACV, nguyen2024diffusiondark pages 4134-4145, blau2018pirm year 2019, li2021zerodcepp = TPAMI 44(8) 2022 | STATUS.md dispatch (2) | writing/icml2024/custom.bib, ralph/phase0_related.md §5
2026-09-23T23:40 | writing | phase0_related.md §5 rewritten from the survey (slices A-F, one line per key, 5.1-5.9); appendix related_works.tex extended with color-space family, HVI follow-ups and evaluation-practice paragraphs over verified keys only, no result claims; remaining unverified: the five unpaired-set dataset papers and the third-party 24.7401 README row | STATUS.md dispatch (2)(3) | writing/section/related_works.tex
2026-09-23T22:57 | master | ACCEPT writing's ff56879 after independent spot-check: gen-table --check exit 1 on MISSING src, 0 with --allow-missing; writing-audit --final exit 1, phase-0 audit exit 0; custom.bib 112 entries (103 survey + 28 prior, deduplicated), 0 undefined citations; all 8 candidates-v5 keys unchanged; hvicidnetplus = TCSVT 36(8) 2026, tpcnet = ECCV 2026 | rule 10: tool author is not its auditor | tooling/gen-table.py, tooling/writing-audit.sh, writing/icml2024/custom.bib
2026-09-24T02:26+00:00 | [human] | Gate 0 criteria changed: new method + specific locus + fast iteration; C1 ladder demoted to ablation chapter | author decision | ralph/INBOX.md
2026-09-24T02:27 | [human] | NEW Gate 0 criteria: thesis = a NEW method component on HVI-CIDNet that improves a SPECIFIC measurable locus, with FAST iteration (fine-tune from released LOL-v1 w_perc weights ~50 epochs ≈25 min/run, val-split metric per iteration, test once at the end, 3 seeds from scratch only for the final table). C1 ladder becomes the ablation chapter. master → M1–M5 method candidates; experiment → fine-tune loop + one-pair ranking check vs a full run; no other runs; topic NOT confirmed | author's decision | ralph/INBOX.md line 18
2026-09-24T02:27 | master | Ranking-check pair for the fine-tune loop = A0 (HVI, learned k) vs A2 (k = 0): both already wired, the contrast is the one R2 marks informative, and a 50-epoch fine-tune vs a 1000-epoch from-scratch pair is the cheapest honest test of whether the loop preserves ranking (2 full runs ≈ 15 GPU-h on 2 GPUs + 2 fine-tunes ≈ 1 h) | author authorised exactly one instrument pair | HVI-PLAN.md §Phase 0
2026-09-24T02:31 | master | phase0_candidates.md v6 (author's 'v5 method candidates'): M1 dark-chroma confidence (recommended), M2 exposure-equivariant I-branch (pivot), M3 HVI-aware supervision (M1's first wave), M4 repaired LCA, M5 illumination-normalised inference; each with locus + file-backed evidence, component, loop plan, named baselines, novelty risk; C1 ladder = ablation chapter; recommendation re-posted in INBOX | author's new Gate 0 criteria | ralph/phase0_candidates.md Part I
2026-09-24T02:33 | [human] | INBOX line 19 + pane note: novelty checks for M1/M2/M3 in ralph/related/novelty/ (all INCREMENTAL; each names closest work, baseline to beat, narrowly novel version); fold into candidates | author's instruction | ralph/related/novelty/
2026-09-24T02:33 | master | candidates v7: adopted the author's numbering (M1 exposure-conditioned, M2 adaptive density k(x), M3 chroma confidence; M4 loss, M5 LCA); each M1–M3 now carries verdict, closest works, strongest baseline, the narrowly novel version, and the check's deciding experiment ported to the 25-min loop with an explicit go/no-go; my former illumination-normalised idea became M1's wrapper baseline. RECOMMENDATION CHANGED: M2 primary (least crowded, provable bound, locus already instrumented), M1 pivot, M3 zero-run comparison; re-posted in INBOX | novelty margin × loop fit; M3's neighbourhood has four 2026 works | ralph/phase0_candidates.md Part I
2026-09-24T03:05 | experiment | Released LOLv1 w_perc weights fetched from huggingface.co/fediory/HVI-CIDNet-LOLv1-wperc @1fa9b699 (model.safetensors sha256 1163da20…) into /home/work/research/weights/LOLv1_wperc; loads into CIDNet with no missing/unexpected keys; released k = 1.1255 (init 0.2); on the scene_v1 validation images (part of its training data) PSNR 28.68 ungated / 26.74 gated, GT-mean 30.86; README eval15 numbers not checked (test split) | master dispatch (author's new Gate 0 criteria) | ralph/results/phase0_weights.json
2026-09-24T03:05 | experiment | Fine-tune mode added: --finetune <weights> --epochs 50, linear warmup 2 epochs to lr 3e-5 then cosine to 1e-7 re-annealed to epoch 50, val every epoch, snapshots every 10, frozen loss, k0 0.2; A2 re-applies k=0 after loading; multi-job runs (one manifest, jobs per arm/seed); study name flag | master dispatch; lr 3e-5 is the recorded choice from the suggested 2e-5..5e-5 | auto-research/hvi_baseline.py
2026-09-24T03:05 | experiment | rankcheck_v1 (finetune + baseline, 4 jobs) FAILED at step 1: the frozen loss's HVI transform stayed on CPU (device mismatch; CPU preflight cannot catch it); fixed (c1_arms.py), verified by one GPU step per arm, relaunched as rankcheck_v2 (new run name, new script hash); v1 dirs kept, marked failed | persona §6/§7 | outputs/{finetune,baseline}/rankcheck_v1
2026-09-24T02:34 | master | FACT: released LOL-v1 w_perc weights have learned k = 1.1255 (init 0.2) → k moves far during upstream training; the A1 (fixed-k) ladder arm stays; scene_v1 val PSNR of the released model = 28.68 ungated / 26.74 gated, i.e. those val images were in its training set | experiment's phase0_weights.json | ralph/results/phase0_weights.json
2026-09-24T02:34 | master | CAVEAT (stated in the plan and the paper): the fast loop fine-tunes from weights that were trained on our val images and monitored on eval15; therefore fine-tune val numbers are memorisation-inflated and are used ONLY to rank variants against an identically fine-tuned upstream control, never as absolute claims; the final table is 3 seeds from scratch | fast iteration was the author's requirement; the clean number exists elsewhere | ralph/results/phase0_weights.json
2026-09-24T02:34 | master | DEFAULT (reversible at Gate 0): loss-side frozen k0 = the released value 1.1255 for fine-tune runs (matches the init the loop starts from) and for from-scratch runs the A0 median converged k once the 5 A0 seeds exist; until then 1.1255 | R2 §1b: k0 constant, detached; choosing the released value keeps the fine-tune loss consistent with the init | R2 §1b
2026-09-24T02:34 | master | rankcheck_v1 failed at step 1 (loss-side transform on CPU); fixed 0cfa155; relaunched as rankcheck_v2 with a new run name — matches the "restart once with the same protocol" default | persona §5 | ralph/RESULTS.md
2026-09-24T02:38 | [human] | INBOX line 21 + pane note: T1–T6 transfer-style candidates checked (NOVEL-IN-HVI: T1, T3, T4, T6 thin; INCREMENTAL: T2, T5); author leaning T1 primary + T3 extension; add to candidates; still no runs | author's instruction | ralph/related/novelty/T*.md
2026-09-24T02:38 | master | candidates v8: T1–T6 added with verdicts, closest works, baselines, loci; go/no-go thresholds are master's proposals (the checks state none); RECOMMENDATION → T1 + T3 (author's lean, NOVEL-IN-HVI, loci measurable on val with no new data), with the stated requirement that T1 pairs equivariant features with an invariant cast estimator (equivariance alone preserves a cast) and that the fair control is augmentation-only; pivot M2; re-posted in INBOX | novelty verdicts × loop fit × locus measurability | ralph/phase0_candidates.md Part I
2026-09-24T02:39 | master | CORRECTION: clock strings written today in STATUS.md, the candidates header and INBOX notes (03:10–04:25) were estimates ≈ 1.5 h ahead; corrected to the real clock; DECISIONS lines were already real (date -u) | audit trail | this line
2026-09-24T03:40 | experiment | Fine-tune rank check (one seed): A0−A2 = +7.7 dB on the scene_v1 validation split, but the released weights were trained with k≈1.13 and A2 replaces the transform by k=0, so the 50-epoch fine-tune measures recovery from a representation mismatch, not the arms' quality; A2 peaks at epoch 7 then declines (lr 3e-5 too high for a mismatched init, or the loop simply cannot adapt in 50 epochs). Interpretation: the fast loop is only valid for method components that keep the released representation (or re-learn it), which is a constraint on the thesis method design; the from-scratch pair (running, ~7 h) shows the opposite sign at epoch 55 (A2 +0.17 dB), not meaningful yet | measured; both loops one seed | ralph/results/phase0_finetune_rankcheck.json (partial until the scratch pair finishes)
2026-09-24T02:57 | master | FACT: 50-epoch fine-tune from released w_perc (k=1.13): A0 best 29.14 dB val, A2 (k=0) 21.42, peaking at epoch 7 then declining → the loop measures recovery from representation mismatch, not the arm (8203d45) | experiment's rankcheck | ralph/results/finetune__rankcheck_v2.json
2026-09-24T02:57 | master | RULING: the fast loop is valid only for release-compatible components (init reproduces the released output; wave-0 preflight required); a null check (control vs no-op) is wave 0 after Gate 0; the C1 ladder ablation runs FROM SCRATCH, default A0/A2/A3 × 3 seeds; the running scratch A0/A2 pair is seed 1 of it | a loop that cannot rank representation changes must not be used for them | ralph/phase0_candidates.md loop section
2026-09-24T02:57 | master | The scratch pair's agreement verdict is retargeted: it now gives the true A0−A2 gap (ladder seed 1), not a validation of the fine-tune loop, which is validated by the null check instead | the original check compared incomparable things | ralph/results/phase0_finetune_rankcheck.json
2026-09-24T04:10 | experiment | --preflight-release added (hvi_baseline.py): loads w_perc into the plain CIDNet and into ArmNet(arm) at init, compares outputs on 5 scene_v1 val images (CPU, identity knobs), PASS iff max|diff| <= 1e-5; fine-tune launches run it automatically and refuse a failing arm unless --allow-mismatch (recorded in the manifest). Measured: A0 0.0 PASS; A2 0.957 FAIL | master's rule: the fast loop is valid only for components whose init reproduces the released output | auto-research/hvi_baseline.py
2026-09-24T04:10 | experiment | phase0_finetune_rankcheck.json relabelled: scratch pair = LADDER SEED 1 (true A0−A2 gap, one seed, not citable); mismatch finding recorded in notes as data; comparison block marked descriptive, not a loop-validity verdict | master dispatch | ralph/results/phase0_finetune_rankcheck.json
2026-09-24T03:00 | master | ACCEPT experiment 1bd9738: --preflight-release asserts a component at init reproduces the released output (A0 diff 0.0 PASS, A2 0.957 FAIL, tol 1e-5, 5 val images); fine-tune launches refuse a failing arm unless --allow-mismatch; rankcheck JSON relabelled (scratch = ladder seed 1) | rule 9: the guard was shown to pass and to fail | auto-research/hvi_baseline.py
2026-09-24T03:02 | [human] | INBOX line 25 + pane note: R3 red-team — T1 as proposed is broken (equivariance passes casts through; real casts are chroma translations/gains, not rotations; a strictly equivariant net cannot output a fixed chroma correction); T1 is NOT the topic; T3 under review; Gate 0 open; no runs | author's decision | ralph/related/redteam/R3_T1_design_redteam.md
2026-09-24T03:02 | master | candidates v9: T1 marked REJECTED with R3's reasons, R3's narrower permitted version recorded (not recommended), R3's bib fact-checks recorded; T3 marked under review; recommendation reverts to M2 (release-compatible at init), T3 as extension only if it survives review, pivot M1; INBOX re-posted | the flagged conceptual risk (equivariance ≠ correction) was confirmed and sharpened by R3 | ralph/phase0_candidates.md Part I
2026-09-24T03:42 | [human] | INBOX line 27 + pane note: R4 (T3), R5 (T6), R6 (T4) — all three fail as methods (P(beat trivial baseline) 0.30/0.20/0.12), survive only as seeded measurement studies; rank the C1 ladder first again with T3's oracle decomposition as an optional zero-training chapter; no runs | author's decision | ralph/related/redteam/R4–R6
2026-09-24T03:42 | master | candidates v10: C1 ladder ranked first (only red-team survivor, instrument built, seed 1 running); T3 kept as R4's GT-oracle decomposition chapter; T4/T6 salvage framings recorded (T4 → C4 seeded loss study; T6 → Pareto KD study); M2 noted as the best remaining method if the author wants one; INBOX re-posted | author's instruction + red-team outcomes | ralph/phase0_candidates.md
2026-09-24T04:51+00:00 | [human] | GATE 0: topic confirmed = C1 nested ladder + loss ladder (advisor framing: reproduction under conditions; loss is heuristic) | author decision after 6 novelty checks + 6 red-teams | ralph/INBOX.md, ralph/related/redteam/
2026-09-24T04:56 | [human] | GATE 0: PASS — topic confirmed: C1 nested HVI ladder (R2 design) + LOSS LADDER as the second axis; advisor framing "confirm under which conditions and by how much HVI-CIDNet's effect reproduces"; primary read-out final ckpt GT-mean PSNR (scalar gain); Gate A = A0 × 5 seeds must reproduce raw 23.3–24.3 with σ ≤ 0.5 before other arms; T3 oracle decomposition as zero-training bonus after launch; per-intensity-bin colour analysis mandatory | author's decision | ralph/INBOX.md line 29
2026-09-24T04:56 | master | HVI-PLAN.md §0–§8 filled (241 lines): §3 frozen protocol (code defaults, scene_v1 split, 5 seeds 42–46, val/5, ckpt/10, k0 = 1.1255 detached, knobs identity, FP32 default mode), §4 S1 loss ladder L0–L4 nested so that L2 = S2's frozen loss, S2 representation ladder A0–A4 under L2, S3 zero-run, S4 T3 oracle; §5 gates A–D with defaults; §6 storyline; §7 provisional values; §8 pivots P-A, P-σ, P-A1, P-A2, P-L, P-mech, P-budget, P-S4, P-neg | plan §Phase 0 step 4 | HVI-PLAN.md
2026-09-24T04:56 | master | DECISION: Gate A arm = A0 under the FULL upstream loss L0 (VGG in both spaces, k coupled), because the reproduction window [23.3, 24.3] is defined for the released recipe; L0 doubles as S1's reference arm. The loss ladder is nested L0→L1 (−VGG_hvi)→L2 (+k detached = frozen loss)→L3 (RGB-only) and L2→L4 (C_k-weighted chroma); the author's "one principled alternative" default (C_k weighting) is kept, uncertainty weighting rejected per R6 K3 | one reproduction arm, one frozen loss, no wasted runs | HVI-PLAN.md §4
2026-09-24T04:56 | master | DECISION: Gate A's test read of the five A0 final checkpoints IS each run's single final eval (final ckpt is the primary read-out, no selection), so rule 4 holds; total budget 45 runs ≈ 350 GPU-h ≈ 3.6 days; the running rankcheck_v2 A0/A2 seed-42 pair is reused as L2-A0/L2-A2 seed 42 only if its manifest hash equals the frozen one | integrity floor rule 4; no rerun of finished work | HVI-PLAN.md §3, §5
2026-09-24T04:56 | master | GATE A: OPEN — launched (A0-L0 seeds 42–46; 2 now on GPU 0/1, 2 when GPU 2/3 free, 1 after); verdict when the five final-eval JSONs exist | plan §5 | ralph/results/
2026-09-24T04:57 | experiment | GATE A launched: gateA/a0_l0_v1 arm L0 (= U: full upstream loss, VGG both spaces, k coupled) seeds 42 (GPU 0), 43 (GPU 1); frozen protocol §3 (scene_v1, 1000 ep, val every 5, snapshots every 10, FP32, identity knobs); manifest script/lib/arms sha 0fd3a438/8ce497ae/e50e6a02; seeds 44/45 follow when the rankcheck_v2 pair frees GPUs 2/3, seed 46 after the first Gate A job completes | master dispatch after Gate 0 PASS | outputs/gateA/a0_l0_v1/manifest.json
2026-09-24T04:57 | experiment | k0 default set to 1.1255 (HVI-PLAN §3) and arm alias L0 added (plan §4 naming; == U); rankcheck_v2 is NOT reusable as L2 seed 42: script/arms hashes differ and its k0 was 0.2 | plan §3 reuse rule | ralph/results/phase0_finetune_rankcheck.json frozen_hash_check
2026-09-24T04:59 | master | Gate A launched (5d47654): L0 seeds 42/43 on GPU 0/1, 27.4 s/epoch ≈ 9 h each; rankcheck_v2 pair NOT reusable (k0 = 0.2 and arm hash differ from the frozen protocol) | experiment's report | ralph/results/phase0_finetune_rankcheck.json
2026-09-24T04:59 | master | DECISION: stop the non-protocol rankcheck_v2 scratch pair now (epoch ≈ 400/1000, k0 = 0.2, not citable) and launch Gate A seeds 44/45 on GPUs 2/3 immediately; partial outputs stay on disk, RESULTS row marked partial | Gate A gates all 40 remaining runs; 9 more GPU-h on an uncitable pair would delay the critical path ≈ 4.5 h; a wall-clock choice, reversible in the sense that nothing is deleted | HVI-PLAN.md §5 Gate A
2026-09-24T05:00 | experiment | Stopped baseline/rankcheck_v2 A0/A2 (SIGTERM to my own workers, pids verified) at epochs 380/378; status.json state=partial with the reason; directories kept; GPUs 2/3 handed to Gate A L0 seeds 44/45 (launched, pids 109043/109102) | master decision (not reusable under the frozen protocol) | outputs/baseline/rankcheck_v2, outputs/gateA/a0_l0_v1/manifest.json
2026-09-24T05:01 | master | Gate A ETA: seeds 42–45 finish ≈ 14:00 UTC (27 s/epoch), seed 46 ≈ 23:00 UTC; verdict when all five final-eval JSONs exist (or on four at 14:00 if seed 46 is late — persona §5: partial at the gate beats complete after it, but the σ estimate then uses n = 4 and is re-stamped at n = 5) | experiment's corrected report | ralph/RESULTS.md
2026-09-24T06:10 | writing | Opening draft written after Gate 0: title, abstract, 7-move intro, method (two ladders, frozen protocol, decision rules, what is not tested), results (Gate A reproduction, S1, k-coupling, S2 + mechanism, S3, transfer, recipe), conclusion, appendix (protocol, per-arm tables, S3 table, mechanism, cross-dataset, S4 oracle-decomposition stub). Body ends p4, References start p5; 0 overfull boxes; phase-0 audit PASS. Every provisional number is a \ph{} from HVI-PLAN.md §7 (or derived by subtraction) and is listed in ralph/PH-LEDGER.md; the two cross-dataset magnitudes I first wrote had no §7 basis and were removed | master's Gate 0 dispatch | writing/section/*.tex, ralph/PH-LEDGER.md
2026-09-24T06:10 | writing (tooling, for master to veto) | gen-table.py: (a) a row may declare "ph": {key: value}; rendered as \ph{} ONLY while the key is absent from the JSON, never bolded, replaced by the measured \phm{} the moment the key exists (--check then reports drift); (b) a column may declare "map" to render analysis vocabulary as paper vocabulary (difference->contributes, equivalent->removable); (c) per-table "size" and "tabcolsep". This keeps tables generated (never hand-typed) while the draft has no visible gaps | writing-first needs \ph tables; "--" cells are visible gaps | tooling/gen-table.py, writing/tables/spec.json
2026-09-24T06:10 | writing (contract proposal for experiment) | Table sources and keys the spec expects: s1__loss_ladder_v1.json and s2__rep_ladder_v1.json with analysis.analysis_s{1,2}.decision = analysis_c1.py output (contrasts.<A-B>.{mean,sd,ci_holm,verdict}) PLUS arm_summary.<arm>.<metric>.{mean,sd} over the five final-checkpoint test reads (not in analysis_c1.py yet); s3__selection_bias_v1.json with analysis.analysis_s3.decision.<arm>.{oracle_minus_final_mean,paper_style_max_over_seeds}; gateA__a0_l0_v1.json for the L0 reproduction row. Rename freely; tell writing the names. Also needed: a k-trajectory key (k per epoch, L0/L1 vs L2), an MDE key, a raw-PSNR contrast for A3-A4, and the cross-dataset sign counts | spec.json _doc | writing/tables/spec.json
2026-09-24T05:20 | experiment (contract answer for writing) | Names and keys as writing's spec, produced by analysis_c1.py (not hashed by any manifest; self-test green): s1__loss_ladder_v1.json and s2__rep_ladder_v1.json with analysis.analysis_s{1,2}.decision.contrasts.<A-B>.{mean,sd,ci_holm.0,ci_holm.1,verdict} (verdict ∈ contributes|removable|inconclusive, plan §4), plus .arm_summary.<arm>.<metric>.{mean,sd} over the five final-checkpoint test reads (metrics psnr, psnr_gtmean, ssim, lpips, delta_e00, delta_e00_gtmean, log_exposure), .contrasts.<A-B>.mde_db (paired MDE at the measured SD), .contrasts.<A-B>.raw_psnr.{mean,sd,ci95} (raw-PSNR contrast, e.g. A3-A4), S2's A0 = S1's L2 runs by alias; s3__selection_bias_v1.json with analysis.analysis_s3.decision.<arm>.{oracle_minus_final_mean,sd,paper_style_max_over_seeds}. gateA__a0_l0_v1.json = export_results (validation keys jobs.L0_seed<s>.{best,last}.<metric>); the Gate A TEST reads live in final_eval__test.json entries gateA_L0_seed<s>.metrics.{psnr,psnr_gtmean,...} and, after the analysis, in s1 arm_summary.L0. PLANNED (needs run dirs / downloads): k_trajectory.<arm>.<seed> sampled every 10 epochs into the s1 JSON; cross-dataset sign counts after LOL-v2-Real dedup | writing's contract proposal 06:10; numbers move only through JSON | auto-research/analysis_c1.py, ralph/results/phase0_analysis_selftest.json
2026-09-24T05:08 | master | ACCEPT: experiment adopted writing's JSON key contract in analysis_c1.py (b5453af; s1/s2/s3 names, analysis.analysis_s*.decision.*, verdict vocabulary, arm_summary, mde_db); Gate A test reads live in final_eval__test.json gateA_L0_seed<s>.metrics; no hashed instrument file touched | one join between paper and JSON, by key | ralph/DECISIONS.md contract lines
2026-09-24T05:09 | master | AUDIT of writing's opening draft (f0cd1fc) by an independent subagent: tooling green (audit PASS, verify-phm 0/0, --check exit 1 on missing src, rule-9 planted drift/bold/typing all caught); 5 findings sent to writing: (1) method.tex:34 round-trip "below 4e-7" is false vs JSON (6.43e-7 preflight; 1e-4 adapters) → \phm by key or delete; (2) text/table inconsistencies: selection-bias 0.6 vs 0.3, A4 raw 23.2 vs 23.0, "two removable" vs inconclusive; (3) result sentences with no §7 basis (LOL-v2-Real transfer, "five brightest deciles show none", "monotonically", "whole" vs "mostly", appendix 0.3 dB knobs) → \ph with a §7 row (two rows added) or delete; (4) bind 0.042 run-to-run floor and 1,975,569 params to keys; 6 → 12 LCA blocks; (5) "a dozen follow-ups" → count cited | persona §7 milestone audit; rule 10 | ralph/DECISIONS.md
2026-09-24T07:05 | writing | Master's audit of f0cd1fc applied in one pass: (1) round-trip-error sentence deleted (6.43e-7 in JSON; scientific notation is not bindable by verify-phm), adapters' preflight file named instead; (2) selection-bias text now 0.3 with a pointer to the S3 table (0.6 removed), A3/A4 raw ph set to 23.2/23.0 so A3-A4 raw = 0.2, "two removable steps" -> "one removable, one inconclusive"; (3) LOL-v2-Real paragraph uses §7's new row (\ph{22.0} dB, \ph{4} of 5 seeds), mechanism sentence softened to the pre-registered rule, "whole" -> "most", S4 knobs ≤ \ph{0.3} kept and now §7-backed; (4) first two \phm bindings: run-to-run floor 0.04 -> phase0_codebase.json reproducibility.epoch2_val_psnr_spread_db, parameter count 1975569 -> phase0_arms_preflight.json ladder_parameter_count (verify-phm 2/2); "six blocks" -> "twelve (six per branch)"; (5) "a dozen follow-ups" -> "follow-ups". All logged in PH-LEDGER.md. Build green, 0 overfull, body ends p4, References p5; the audit's conclusion-tail locator now warns (pdftotext line-wrap), verified by hand | master audit | writing/section/*.tex, ralph/phm-spec.json, ralph/PH-LEDGER.md, writing/tables/spec.json
2026-09-24T05:13 | master | ACCEPT writing's audit fixes (9e6891a) after direct re-check: round-trip sentence deleted, text/tables consistent, LOL-v2-Real and S4 values on §7 rows, verify-phm 2/2 by key, audit PASS, body p4 / refs p5, 10 pages | persona §7 | writing-main.pdf
2026-09-24T05:16 | master | ACCEPT fig_ladders.py (b3a8b88): 3-panel lead figure from the contract keys, sources sha256 in PDF metadata + sidecar, grayscale-safe verdict markers, synthetic test kept out of writing/figures | ruling 6 (sources fingerprints); plan §6 lead figure | auto-research/fig_ladders.py
2026-09-24T12:48 | experiment | Gate A seed 42 complete (1000 ep, 27 s/epoch); one test read of last.pt via final_eval_test.py (label gateA_L0_seed42): raw 22.78 dB / GT-mean 26.52 on eval15 — 0.5 dB below the [23.3, 24.3] window for this one seed; gate decided on the 5-seed mean; final k = 0.850 (released 1.1255, init 0.2); seed 46 launched on GPU 0 (pid 205711) into the same manifest | plan §5 Gate A | ralph/results/final_eval__test.json
2026-09-24T12:43 | master | Gate A seed 42 (903200b): eval15 final ckpt raw 22.78 / GT-mean 26.52 / SSIM 0.836 / LPIPS 0.112; val best 23.07 @430, last 22.51; final k = 0.850 — 0.5 dB below the window's lower edge; gate is on the 5-seed mean, no stamp yet | plan §5 | ralph/results/final_eval__test.json gateA_L0_seed42
2026-09-24T12:43 | master | PRE-COMMITTED K0 procedure if the Gate A mean is < 23.3: the one debug cycle is ZERO-TRAINING first — on the five A0-L0 runs compute (a) the oracle max over saved checkpoints (paper-style selection), (b) the gated (×1.3 saturation) metric of the final checkpoint, (c) the val-selected checkpoint's test PSNR; if (a)+(b) lands in [23.3, 24.3] the pipeline reproduces upstream's REPORTING conditions and the shortfall is protocol (final ckpt, 445 vs 485 images, ungated) → GATE A: PASS-with-P-A (report the decomposition as the first result; all ladders proceed). Only if even oracle-max + gated misses the window does a bug become plausible → one upstream-exact replication run (485 train, seed 42) before any other arm | R2 rule 6, plan §8 P-A; the instrument's loss/metrics were proven bitwise equal to upstream | HVI-PLAN.md §5, §8
2026-09-24T13:05 | experiment | Gate A seed 44 complete: eval15 final raw 23.97 / GT-mean 26.90 (inside the window); K0 reads written to final_eval__test.json with kind+notes (gated, valsel; oracle to final_eval__oracle.json): seed 42 gated 23.32, valsel 22.68; seed 44 gated 24.43, valsel 23.94; oracle seed 42 running on GPU 2 | master's K0 procedure | ralph/results/final_eval__test.json
2026-09-24T13:00 | experiment | Gate A seed 43 complete: eval15 final raw 23.36 / GT-mean 26.52; gated 23.73; valsel (best.pt ep 215, val 22.66) reads 21.28 on test — val-selection picked an early checkpoint that is 2.1 dB worse on test than the final one; oracle seed 43 running on GPU 1 | K0 procedure | ralph/results/final_eval__test.json
2026-09-24T12:54 | master | Gate A running tally (final ckpt, eval15 raw): s42 22.78, s44 23.97, s43 23.36 → mean 23.37, SD 0.60 at n=3. PRE-COMMIT for the n=4 provisional stamp: mean in [23.3, 24.3] and SD ≤ 0.5 → PASS; mean in window and 0.5 < SD ≤ 0.8 → PASS-provisional: ladders launch AND seeds 47–49 are added to A0-L0 (then A2, A3 per R2) so σ is re-estimated at n = 8 — an SD estimate from 4 seeds is too noisy to stop 40 runs on; mean below 23.3 → K0 zero-training procedure (oracle + gated) as logged; re-stamped at n = 5 when seed 46 lands | persona §5: partial at the gate beats complete after it; reversible (extra seeds cost 3 runs) | ralph/results/final_eval__test.json
2026-09-24T12:56 | master | GATE A: PASS (provisional, n = 4; re-stamp at n = 5 when seed 46 lands ≈ 21:40 UTC) — A0-L0 final-checkpoint eval15 raw PSNR {22.78, 23.97, 23.36, 23.27}: mean 23.34 dB ∈ [23.3, 24.3], seed SD 0.49 ≤ 0.5; GT-mean 26.63 ± 0.18. Addendum (0.4 < SD ≤ 0.5, R2/plan §5): seeds 47–49 added to A0-L0, A2, A3. K0 decomposition reads (not for selection): gated final 23.83 ± 0.56 raw / 27.76 ± 0.26 GT-mean (n = 3) — upstream's README 23.81 / 27.71 reproduces under its own reporting conditions; val-selected 22.63 ± 1.33 (n = 3; val picks early checkpoints for some seeds) | plan §5 Gate A; first S3 result | ralph/results/final_eval__test.json
2026-09-24T12:56 | master | DISPATCH S1 + S2 as one ordered queue, one job per free GPU (GPU 0 busy with seed 46 until ≈ 21:40): L2 × {42..46} first (S2's frozen-loss reference and the source of A1's k median), then A0-L0 × {47, 48, 49}, then L1, L3, L4 × {42..46}, then A2, A3 × {42..49}, A4 × {42..46}, A1 × {42..46} last (needs the A0-L2 k median). Each completion → export + single final read; oracle passes post hoc in one dedicated pass per arm after its seeds finish | plan §4 order of dependencies; keep GPUs full | HVI-PLAN.md §4
2026-09-24T13:30 | writing | First measured numbers bound (Gate A, n=4): \phm{4} <- gateA__a0_l0_v1.json n_jobs.complete; per-seed min/max raw PSNR 22.78/23.97, GT-mean 26.52/26.90, gated+GT-mean 27.55/28.05 <- final_eval__test.json entries.gateA_L0_seed*.metrics.*; new appendix table app_gatea_seeds (16 cells, per seed: raw, GT-mean, gated, val-selected) generated from the same file. verify-phm 25/25, 0 unbacked. NO aggregate key exists in either file, so the means/SDs in the reproduction paragraph stay \ph (23.3 / 0.5 raw, 26.6 / 0.2 GT-mean, hand-computed from the four reads and marked so in PH-LEDGER); the headline GT-mean gain moved 3.9 -> 3.3 (\ph) everywhere for consistency. app_s3 untouched: no oracle file exists yet; the val-selected reads are in the Gate A table instead. Ladder tables untouched | master's binding dispatch | writing/section/experiments.tex, writing/tables/app_gatea_seeds.tex, ralph/phm-spec.json, ralph/PH-LEDGER.md
2026-09-24T13:30 | writing (request to experiment) | To bind the means: add an aggregate per label group to final_eval__test.json (e.g. summary.gateA_L0.{psnr,psnr_gtmean,...}.{mean,sd,n} over kind=final entries) or an analysis block in gateA__a0_l0_v1.json; the same shape (arm_summary.<arm>.<metric>.{mean,sd}) is what the ladder spec already expects | verify-phm binds one key per number; a mean is not a key | ralph/phm-spec.json
2026-09-24T12:59 | master | ACCEPT writing's Gate A binding: verify-phm 25/25 by key, per-seed values + new app_gatea_seeds table bound; means/SDs correctly left as ph until experiment exports summary keys (ruling 3: derived stats live in JSON, never chat arithmetic) | rule 2, ruling 3 | ralph/phm-spec.json
2026-09-24T13:20 | experiment | Gate A seed 45 complete: eval15 final raw 23.27 / GT-mean 26.59; gated 23.70; valsel 22.56. Four final reads: mean 23.344 dB, SD 0.490 (seed 46 running) | K0 procedure; master's provisional PASS at n=4 | ralph/results/final_eval__test.json
2026-09-24T13:20 | experiment | Ladder launch prep: c1_arms.py now carries the plan's names (L0..L4, A0..A4; old 'L1' rgb-only arm renamed L3), losses coupled_novgg (L1) and frozen_ckw (L4: H,V of both tensors × C_k0(I_gt)), A1 = hvi_kfixed (k_fixed recorded in the manifest); hvi_baseline gains --k-fixed and --job-suffix; analysis_c1 writes decision.json into the run dir so export_results produces the single contract file. Consequence: c1_arms hash changed → Gate A seeds 47-49 run in gateA/a0_l0_v2 (identical protocol, new manifest), not v1 | master dispatch (ladders as one ordered queue) | auto-research/queue.py
2026-09-24T13:03 | master | Ladder queue launched (445a2d1): daemon ladder_queue.py, one job per free GPU, per-completion export + single final read, per-arm oracle after all seeds, A1 k from the L2 median, one retry then drop (matches persona §5). Gate A n = 4 confirmed: 23.344 ± 0.490. gateA__summary_v1.json written (k_final median 0.847) | plan §4 | ralph/results/gateA__summary_v1.json
2026-09-24T13:03 | master | RULING: A0-L0 seeds 47–49 run as gateA/a0_l0_v2 (c1_arms hash changed by new arm code; protocol identical) and may be POOLED with v1 seeds 42–46 only if the manifest records (a) identical config and (b) a preflight showing the L0 forward+loss path of the v2 code reproduces a v1 checkpoint's forward/loss on fixed inputs to float tolerance; otherwise they are reported as a separate group | plan §2: a protocol change is a new run name; a code-only change needs proof it is code-only | outputs/gateA/a0_l0_v2/manifest.json
2026-09-24T14:05 | writing | Gate A means bound by key from gateA__summary_v1.json: raw 23.34 ± 0.49, GT-mean 26.63 ± 0.18, gated+GT-mean mean 27.73 (README 27.71 stays \ph as literature), n=4 (also bound to summary n), learned-k median 0.85 (replaces the §7 1.1 in the L0 context; the A0-L2 k value stays \ph{1.1} until S2 lands). verify-phm 32/32, 0 unbacked; audit PASS; body p4 / refs p5. Not changed: abstract/intro σ_seed \ph{0.3} (the ladders' planned seed SD; measured L0 test SDs are 0.49 raw / 0.18 GT-mean, master to rule whether the headline σ should follow) and the 3.3 gain (a difference of two keys, not a key) | master's dispatch | writing/section/experiments.tex, writing/section/introduction.tex, ralph/phm-spec.json, ralph/PH-LEDGER.md
2026-09-24T13:05 | master | RULING: the headline seed SD in abstract/intro is the PRIMARY metric's (GT-mean PSNR) and is bound by key to summary.gateA_L0.final.gtmean_psnr.sd (0.18 at n = 4), stated as "on GT-mean PSNR"; the raw SD (0.49) is cited by its own key where raw PSNR is discussed; the plan's ph 0.3 is retired wherever a key exists; headline numbers identical across abstract, intro, experiments | rule 1–2: a ph never ships where a measured key exists | ralph/results/gateA__summary_v1.json
2026-09-24T13:05 | master | RULING: the "GT-mean rescaling gain" (GT-mean − raw, ≈ 3.3 dB) and every other difference of two keys is exported by experiment as its own key (summary.gateA_L0.decomposition.{gtmean_minus_raw, gated_minus_ungated, valsel_minus_final, oracle_minus_final}.{mean,sd,n}) before the paper cites it; until then it stays ph | ruling 3 and 8: derived stats and ratios live in JSON with their denominators | ralph/results/gateA__summary_v1.json
2026-09-24T13:05 | master | ACCEPT pooling proof (5282697): v1 code vs current code on arm L0, v1 checkpoint, 5 fixed crops, CPU — eval output, train output, loss, 8 terms, all gradients incl. k bitwise equal (max diff 0.0); a0_l0_v2 manifest records poolable = true with the computed protocol diff → seeds 47–49 pool with 42–46 as one A0-L0 group | ruling of 13:05 satisfied | ralph/results/gateA__v2_equivalence.json
2026-09-24T13:06 | master | ACCEPT gateA__summary_v1.json regeneration with decomposition keys and a sources fingerprint (5298ba2); oracle_minus_final populates after the 43–45 passes | ruling 3, 6 | ralph/results/gateA__summary_v1.json
2026-09-24T14:40 | writing | Master rulings applied: headline seed SD is now \phm{0.18} (summary.gateA_L0.final.gtmean_psnr.sd, GT-mean = primary metric) in abstract, intro, conclusion and results, with the raw SD \phm{0.49} cited by its own key where raw PSNR is discussed; the plan's \ph{0.3} seed SD is retired (the remaining \ph{0.3} are the selection-bias and S4 rows). 3.3 dB gain and every difference of two keys stay \ph until summary.gateA_L0.decomposition.* lands (ledger row added). verify-phm 33/33, 0 unbacked; body p4 / refs p5 after trimming two sentences | master rulings | writing/section/{abstract,introduction,conclusion,experiments}.tex, ralph/phm-spec.json, ralph/PH-LEDGER.md
2026-09-24T15:10 | writing | Decomposition keys bound: rescaling gain \phm{3.29} (gtmean_minus_raw.mean) identical in abstract/intro/results/appendix; gate adds \phm{1.10} dB GT-mean (gated_minus_ungated_gtmean.mean) in the reproduction paragraph; validation-selected minus final \phm{-0.39} dB GT-mean in the appendix Gate A text. oracle_minus_final stays \ph (n=1). verify-phm 36/36, 0 unbacked; body p4 / refs p5 after trimming three phrases | master's dispatch | writing/section/{abstract,introduction,experiments,appendix}.tex, ralph/phm-spec.json, ralph/PH-LEDGER.md
2026-09-24T13:08 | master | ACCEPT writing's decomposition binding: GT-mean gain 3.29, gate +1.10 GT-mean, valsel−final −0.39 GT-mean all by key; oracle stays ph at n = 1; verify-phm 36/36. Next master audit (persona §7 fan-out, one subagent per numeric claim) scheduled at the Gate A n = 5 re-stamp | rule 2 | ralph/phm-spec.json
2026-09-24T15:25 | writing | CORRECTION: commit 7fec331 was pushed with the audit FAILING on the 4-page limit (conclusion spilled to p5) and I reported "audit PASS" to master; my commit chain gated on a grep of the audit output instead of its exit code. Fixed here by cutting two sentences in results (body p4, refs p5, audit exit 0). From now on the chain gates on the audit's exit code | integrity: never push a failing audit, never misreport | writing/section/experiments.tex
2026-09-24T13:08 | master | writing pushed 7fec331 with the audit failing (body on p5) and self-corrected; master re-ran the audit on the fixed tree (result below) — every writing PASS claim is re-run by master before it counts | rule 10 | writing-main.pdf
2026-09-24T13:28 | experiment | Idle GPUs 1/2 (13:05-13:26): my oracle chain for Gate A seeds 44/45 waited on a pgrep pattern that matched its own shell, so it never started and its GPU hold files kept the queue daemon off GPUs 1/2; stuck shells killed, GPU 1 released (L2_seed43 launched 13:26), oracles 44+45 now run sequentially on GPU 2 only and release it when done; the daemon and its free-GPU detection were correct | master's check at 13:24 | outputs/queue/events.log
2026-09-24T13:26 | master | INCIDENT: GPUs 1/2 idle ≈ 13:00–13:26 UTC — experiment's Gate A oracle chain (seeds 44/45) deadlocked on its own pgrep pattern and its GPU hold files kept the daemon off both GPUs; fixed (f85b79a): L2_seed43 launched on GPU 1, oracles 44/45 sequential on GPU 2 then released to the daemon. Cost ≈ 0.9 GPU-h | master's sweep caught it from nvidia-smi, not from the daemon's own health signal — experiment to add a daemon log line when a GPU is free but not filled | ralph/DECISIONS.md
2026-09-24T13:57 | master | S3 (n = 4, A0-L0): oracle max-over-checkpoints 23.70 ± 0.50 raw; paper-style max over seeds 24.30 raw / 26.96 GT-mean; selection bias oracle − final = +0.35 ± 0.08 dB; with the gate (+1.10 GT-mean) and GT-mean rescaling (+3.29) the published LOL-v1 range is reconstructed from the raw final-checkpoint 23.34 (1d6f926). All 4 GPUs on the queue since 13:57 | first measured S3 claim; C-S3 stated in the vocabulary of plan §4 | ralph/results/gateA__summary_v1.json
2026-09-24T16:20 | writing | Oracle keys bound (n=4): selection term is now \phm{0.06} dB GT-mean (decomposition.oracle_minus_final_gtmean.mean) identical in abstract, intro and results, with the raw \phm{0.35} beside it in results; app_s3's L0 row reads by key from gateA__summary_v1.json (0.06, paper-style max 26.96) through a new per-row src/keys override in gen-table.py; the A0-A4 rows stay \ph until the S3 export. verify-phm 40/40, 0 unbacked; audit exit 0; body p4 / refs p5. Note for the storyline: under GT-mean, test-split selection is worth 0.06 dB, not the 0.3 the plan assumed; the gap to the published number is the gate (1.10) and the gain (3.29) | master's standing instruction | writing/section/{abstract,introduction,experiments}.tex, writing/tables/spec.json, tooling/gen-table.py, ralph/phm-spec.json, ralph/PH-LEDGER.md
2026-09-24T13:58 | master | RESULT vs STORYLINE: selection bias (oracle − final) = +0.35 dB raw but +0.06 dB GT-mean at n = 4 — under the primary metric the test-selection effect is within seed noise; the storyline's "selection y dB" move is revised to "selection inflates raw PSNR by 0.35 dB and GT-mean PSNR by 0.06 dB; the published spread is mostly GT-mean rescaling (+3.3) and the saturation gate (+1.1)". Plan §7's provisional 0.3 stays as history; the paper cites the keys (40/40 verified, audit re-run by master exit 0) | persona §5: a result contradicting the storyline → writing revises the storyline; the number does not move | ralph/results/gateA__summary_v1.json
2026-09-24T17:00 | writing | Storyline revision (master's ruling): abstract, intro move 2, the key-observation paragraph, contribution (4) and the S3 paragraph now say that test-split selection inflates raw PSNR by \phm{0.35} dB but GT-mean PSNR by only \phm{0.06} dB, so the published spread is mostly the GT-mean gain (\phm{3.29}) and the saturation gate (\phm{1.10}); all four numbers by key in every place they appear (only_in lists widened). Four paragraphs compressed (k coupling, transfer, recipe, what-is-not-tested) to hold the body on p4. verify-phm 40/40, 0 unbacked; audit exit 0 | master's ruling after the oracle binding | writing/section/{abstract,introduction,experiments,method}.tex, ralph/phm-spec.json, ralph/PH-LEDGER.md
2026-09-24T14:00 | master | ACCEPT storyline revision (selection 0.35 raw / 0.06 GT-mean; spread = gain 3.29 + gate 1.10), audit re-run exit 0, 40/40 by key | rule 2, rule 10 | writing/section/*.tex
2026-09-24T20:26 | master | GATE A: PASS-with-P-A (final, n = 5) — A0-L0 final-checkpoint eval15 raw PSNR {22.78, 23.97, 23.36, 23.27, 22.89}: mean 23.25 dB, SD 0.47; GT-mean 26.65 ± 0.16. The raw mean is 0.05 dB BELOW the window [23.3, 24.3]; the pre-committed zero-training K0 check lands INSIDE it under upstream's reporting conditions: gated final 23.79 ± 0.46 raw / 27.74 ± 0.22 GT-mean (n = 4; README 23.81 / 27.71), oracle max-over-checkpoints 23.70 ± 0.50 (n = 4). Verdict: the pipeline reproduces upstream's reporting conditions; the shortfall is protocol (final checkpoint, ungated, 445 vs 485 training images) → P-A applies: the paper's first result is the measured reproduction and its decomposition, every absolute number labelled "under our protocol"; all ladders proceed. SD in (0.4, 0.5] → seeds 47–49 on A0-L0 (seed 47 launched 20:25 on GPU 2), A2, A3 as already queued | plan §5 Gate A + §8 P-A, K0 procedure logged 12:35 | ralph/results/final_eval__test.json, gateA__summary_v1.json
2026-09-24T20:30 | experiment | GATE A complete, five seeds: eval15 final raw 23.254 dB (SD 0.470), GT-mean 26.645 (SD 0.160); window [23.3, 24.3] and SD <= 0.5 → both satisfied at n=5 (master decides); k_final median 0.850; seed 46 oracle running on GPU 0, then GPU 0 releases to the queue (L0 seeds 47-49 in a0_l0_v2) | plan §5 Gate A | ralph/results/gateA__summary_v1.json
2026-09-24T20:43 | master | gateA__summary_v1.json at n = 5 (bd0fe1d): final/gated/valsel/oracle reads for seeds 42–46, decomposition keys over 5 seeds; GPU 0 back to the queue | Gate A closed; writing re-binds | ralph/results/gateA__summary_v1.json
2026-09-24T20:44 | master | Not a stall: seed 46 oracle pass (≈700 MB) ran on GPU 0 until 20:43 below the daemon's free-GPU memory threshold, so STALL lines were false; hold file protected the GPU; L0_seed48 launched 20:44. Hardening: held_eval.py wraps evals with hold-in-finally (7398f8c). Note for the daemon: a STALL line while a hold file exists should say 'held (eval running)' not 'stall' | correct diagnosis beats a fast one | outputs/queue/events.log
2026-09-24T18:10 | writing | Gate A re-bound at n=5 (11 values flagged by verify-phm, all replaced by key): n=5, raw 23.25±0.47, GT-mean 26.65±0.16, gain 3.39, gate 1.09, valsel-final -0.71, oracle raw 0.41 / GT-mean 0.06, k median 0.85 unchanged, oracle raw read 23.66 newly bound; seed 46 row added to app_gatea_seeds (20 cells). P-A framing applied in the reproduction paragraph: raw final mean sits \ph{0.05} dB (arithmetic, ledger row) below the pre-registered 23.3-24.3 window while the oracle (23.66) and gated+GT-mean (27.55-28.05, mean 27.73) reads sit inside it and match the README; shortfall named as protocol (final checkpoint, no gate, 445 training images); "under our protocol" stated in results, abstract and the absolute-value table captions; §4 vocabulary kept. verify-phm 45/45, 0 unbacked; audit exit 0; body p4 (conclusion ends near the foot of p4; no slack left), refs p5 | master's P-A dispatch; HVI-PLAN §8 P-A | writing/section/{abstract,introduction,experiments,conclusion,appendix}.tex, writing/tables/spec.json, ralph/phm-spec.json, ralph/PH-LEDGER.md
2026-09-24T20:48 | master | MILESTONE AUDIT of c8c1b6c (3 section auditors + master's tooling run): all 45 bound values match their keys; tooling green; hardware statement correct. Defects sent to writing: (1) experiments.tex places the gated GT-mean 27.73 inside the RAW window — the in-window gated read is the gated raw mean 23.67 (key exists in the summary, not yet in phm-spec); (2) abstract labels the paper's own 28.2 as third-party; (3) intro's 23.25 lacks "under our protocol"; (4) "one hundred checkpoints" is 101 by key; (5) "equivalent" is outside the vocabulary; (6) 0.16 is the seed SD, not "selection and seed noise"; (7) conclusion omits the gate term the results include; (8) Gate A paragraph must state the SD criterion (0.47 ≤ 0.5) and the extra-seed rule; (9) bind 445, 27.71, 101, and the 0.05 window gap (derived key requested from experiment); (10) "twelve LCA blocks" must note the I-branch block that receives no gradient; (11) captions: "under our protocol" + seed count + final-checkpoint read-out on app_gatea_seeds and app_s3, app_s2_arms self-contained, bold promise only once measured. Provisional S1/S2 sentences stay as-if-final by the placeholder convention until their JSONs exist | persona §7 | ralph/DECISIONS.md
2026-09-24T19:00 | writing | Master's audit of c8c1b6c applied (12 items): gated RAW mean \phm{23.67} (gated.raw_psnr.mean) is the read inside the raw window, GT-mean 27.73 moved beside the README \phm{27.71} (now bound to phase0_codebase readme key); abstract splits paper 28.2 from third-party 23.5-24.0; "under our protocol" added to the intro key-observation; "one hundred checkpoints" -> \phm{101} (oracle n_checkpoints); "equivalent" -> "removable"; S3 paragraph lists selection \phm{0.06} and seed noise \phm{0.16} separately; conclusion names rescaling, gate, selection, seed noise; Gate A paragraph states SD 0.47 <= 0.5 passes and the 0.4-0.5 rule adds seeds 47-49 (running); \phm{445} bound to phase0_val_split_scene n_train (method + results); window gap bound as \phm{-0.05} (window.final_raw_mean_minus_lower); method notes the dead I-branch LCA block; captions of app_gatea_seeds/app_s3/app_s2_arms state protocol, seed count, read-out and promise bold only for measured cells; gen-table MISSING message corrected. Reading-guide sentence dropped to keep p4. verify-phm 50/50, 0 unbacked; audit exit 0 | master audit | writing/section/*.tex, writing/tables/spec.json, tooling/gen-table.py, ralph/phm-spec.json, ralph/PH-LEDGER.md
2026-09-24T20:51 | master | ACCEPT 176ca09 after master's direct re-check of all 12 audit items (gated raw 23.67 in window by key; 27.71/445/101/−0.05 bound; abstract split; vocabulary; conclusion gate term; captions; Gate A SD rule; protocol label present in abstract, intro, experiments); verify-phm 50/50, audit exit 0, refs on p5 — milestone draft is fit for the author to read | persona §7 | writing-main.pdf
2026-09-25T02:27 | master | S1 arm L2 (frozen loss = S2 reference) complete at n = 5: eval15 final raw {23.05, 23.13, 23.68, 23.01, 23.67}, GT-mean {26.59, 26.87, 26.83, 26.61, 26.77} (per-seed reads from experiment's completion lines; the paper cites only exported keys). Queue: A0-L0 extra seeds 47–49 running, then L1 × 5. Per-arm oracle pass for L2 follows automatically; A1's k_fixed = median final k of these five runs, recorded by the daemon | plan §4 S1/S2 | ralph/RESULTS.md
2026-09-25T03:48 | master | FACT: A1 k_fixed = 0.2937 (median final k of the five A0-L2 runs, k detached in the loss) vs L0 median 0.85 (k coupled) and released 1.13 — k drifts far more when the loss transform is coupled (C-S1b evidence, to be cited by key once exported). k moved 0.2 → 0.29 > 0.05, so pivot P-A1 does NOT fire: A1 stays in the queue | plan §4 S2 A1 rule, §8 P-A1 | outputs/s2 manifest (k_fixed) / s1__loss_ladder_v1.json
2026-09-25T03:50 | experiment | FACT: A1 k_fixed = 0.2937 = median final k of the five L2 runs (frozen loss, k detached at k0 = 1.1255). Under the coupled L0 loss the five Gate A runs ended at median k = 0.847 (released weights 1.1255; init 0.2): with the loss-side path removed, k barely leaves its init, so most of the upstream k drift is loss-side coupling (R2 §1b's chroma-collapse shortcut), not the input-transform path. Recorded in outputs/queue/queue.json and the A1 manifest at launch | queue daemon, L2 arm complete | ralph/results/s1__loss_ladder_v1.json (metrics.csv k column per job)
2026-09-25T03:49 | master | ACCEPT k export (d2d84ab): analysis.analysis_k.decision.summary.<arm>.k_final.* + constants; L0 median 0.847, L2 0.2937; daemon re-exports at every completion | ruling 3 | ralph/results/s1__loss_ladder_v1.json
2026-09-25T04:50 | writing | k statistics bound from s1__loss_ladder_v1.json analysis_k: coupled (L0) k converges 0.2 -> median 0.85, detached (L2) -> median 0.29, released 1.13 (constants.k_init/k_released; summary.{L0,L2}.k_final.median, n=5), in the loss-ladder paragraph and the S2 A1-rule sentence (k moves 0.09 > 0.05 in A0-L2, so A1 stays). The plan's \ph{1.1} for A0-L2 is retired. L1-L2 PSNR verdict stays \ph. Five sentences trimmed (intro angle, results S2/transfer/S3/Gate A) to hold p4; the audit gate stopped two earlier overflowing attempts. verify-phm 54/54, 0 unbacked; audit exit 0 | master's dispatch | writing/section/{introduction,experiments}.tex, ralph/phm-spec.json, ralph/PH-LEDGER.md
2026-09-25T03:51 | master | ACCEPT k-statistics binding (verify-phm 54/54, audit re-run exit 0): coupled k median 0.85 vs detached 0.29 stated as measured k values; L1−L2 PSNR verdict stays provisional | rule 2 | writing/section/experiments.tex
2026-09-25T04:07+00:00 | [human] | Agents switched to Opus 5.5 until Fable reset Sat 05:00 KST; pivot/plan decisions held for the author | quota | ralph/INBOX.md
2026-09-25T04:07 | [human] | Model switch: all agents on Opus 5.5 until Sat 2026-09-27 05:00 KST (= Fri 2026-09-26 20:00 UTC). Execute the plan as written (queue, exports, binding, audits); routine PASS stamps per pre-registered rules allowed; any FAIL/pivot/plan or claim change → INBOX Open question, held until the author is back on Fable | author's instruction | ralph/INBOX.md line 37
2026-09-25T04:13 | master | Regenerating gateA__summary_v1.json at n = 6 (5151b66) moved 7 bound keys: final raw mean 23.33 is now INSIDE the window (gap +0.02), which contradicts the paper's pre-registered Gate A statement (n = 5: 0.05 below → P-A). verify-phm 47/54, 7 mismatched. Which n the paper reports is a CLAIM change → HELD for the author (INBOX Open question, rule of INBOX line 37). Interim, reversible, claim-neutral: the n = 5 Gate A evidence is frozen as its own file (gateA__summary_n5_v1.json, same keys, sources = seeds 42–46) and the paper re-points to it with no text change; the pooled summary keeps updating in gateA__summary_v1.json | integrity: bound numbers must match their keys; a gate's evidence must not drift after the stamp | ralph/results/
2026-09-25T05:30 | writing | All gateA_L0 bindings (19 prose entries + the app_s3 L0 row) re-pointed from gateA__summary_v1.json (now n=6) to gateA__summary_n5_v1.json (pre-registered n=5, seeds 42-46); no text or number changed. Which n to report is held for the author | master heads-up | ralph/phm-spec.json, writing/tables/spec.json, ralph/PH-LEDGER.md
2026-09-25T04:16 | master | ACCEPT re-point of Gate A bindings to gateA__summary_n5_v1.json (verify-phm and audit re-run by master, below); paper text unchanged; reporting-n question still HELD in INBOX | integrity | ralph/phm-spec.json
2026-09-25T04:17+00:00 | [human] | master always on Fable 5.1 (pauses allowed); experiment/writing on Opus 5.5; wake-up nudge at quota reset | author | ralph/INBOX.md
2026-09-25T04:40 | experiment | Author note (INBOX): master may be paused until Sat 05:05 KST. Experiment continues autonomously per the plan: ladder_queue daemon keeps launching in dispatch order (one job per free GPU), each completion gets export + single eval15 final read + RESULTS.md row; per-arm oracle passes after all seeds; gateA__summary_v1.json regenerated after L0 seeds 48/49; artifacts committed after each event. Held for master's ruling: n=5 vs pooled Gate A reporting, any ladder cut or protocol change, any failed-twice cell | author note | ralph/INBOX.md
2026-09-25T09:03+00:00 | [human] | Author away until Thu 2026-10-01: standing instructions 1-5 in INBOX (master decides pre-registered pivots; no new scope; final draft + audit + AUTHOR_BRIEF.md, not marked complete) | author | ralph/INBOX.md
2026-09-25T08:45 | experiment | Author away until Thu 2026-10-01 (INBOX). Experiment keeps executing the plan without waiting: ladder_queue daemon (38 jobs queued at 08:45; ≈ 7.5 GPU-h each on 4 GPUs → queue expected to drain ≈ 2026-09-28), per-completion export + single eval15 final read + RESULTS.md row + commit, per-arm oracle passes, gateA__summary_v1.json regenerated after L0 seed 49, and — once an arm set is complete — the frozen analysis_c1.py writes the S1/S2/S3 decision JSONs (Gate B/C inputs), exported and committed. Still held for a ruling (master or author): n=5 vs pooled Gate A reporting, any cut/protocol change, any failed-twice cell, Gate B/C verdicts themselves | author note | ralph/INBOX.md
2026-09-25T09:10 | writing | s1__loss_ladder_v1.json analysis_k now pools L0 k over n=7 (seeds 47-48, v2), median 0.84; the 'over five seeds' L0 k sentence re-pointed to gateA__summary_n5_v1.json k_final.median (0.85, n=5) per master's n=5 ruling. No text change. Which n to report stays the author's call | binding drift | ralph/phm-spec.json
2026-09-25T09:30 | writing (proposal) | Literature numbers (28.2; 23.5-24.0; 0.2-0.4) cannot be bound by a result key and would fail --final; two options logged in INBOX.md, default = a literature_v1.json from experiment so they bind as \phm | final-audit rule | ralph/INBOX.md
2026-09-25T20:30 | [human] | INBOX lines 41–42: master stays on Fable permanently (pauses when quota is exhausted; nudge scheduled Sat 05:05 KST); experiment/writing on Opus 5.5 keep executing. AUTHOR AWAY until Thu 2026-10-01: no answers will come; master takes every decision covered by the plan's pre-registered rules and §8 pivots and logs it; no new studies/arms/datasets/big claim changes (proposals only); zero-training analyses in the plan allowed; when training is done → final evals, analysis, final draft, full milestone audit, push; do NOT mark the paper complete; write ralph/AUTHOR_BRIEF.md | author's standing instructions; supersedes the hold rule of line 37 | ralph/INBOX.md
2026-09-25T20:30 | master | Master paused ≈ 04:30–20:28 UTC (Fable quota). Queue ran unattended: A0-L0 seeds 47–49, L1 × 5, L2 × 5, L3 seeds 42–43 exported with single final reads and oracle passes; L3 44–46 and L4 42 training; L4 43–46 queued. S1 complete ≈ Sat 2026-09-26 13:00 UTC → Gate B; S2 (26 runs) ≈ Mon 2026-09-28 → Gate C | resume from files, no rerun | outputs/queue/events.log
2026-09-25T20:30 | master | RULING (held item, now mine under instruction 2): Gate A stays stamped on the pre-registered n = 5 (gateA__summary_n5_v1.json: raw 23.25, 0.05 below the window → P-A). The extra seeds 47–49 were pre-registered for the σ re-estimate, so the A0-L0 REFERENCE-ARM statistics in the results/tables use the pooled n = 8 (gateA__summary_v1.json: raw 23.3x, inside the window); the Gate A paragraph states both, each by key, and the S1/S2 paired contrasts use seeds 42–46 (matched across arms). Neither number is chosen for its sign; both are reported | pre-registration decides which n serves which purpose | ralph/results/gateA__summary_n5_v1.json, gateA__summary_v1.json
2026-09-25T20:30 | master | RULING (writing's proposal): literature numbers that can never be measured here (paper 28.2; third-party 23.5–24.0; seed SD 0.2–0.4 from shafi2026iphoneblur) are bound as \phm to ralph/results/literature_v1.json produced by experiment with value, source, URL/DOI, verified_by and date per entry; the sentence attributes each with \citep and the word "reported" | keeps "every number is a key" and the --final audit strict | ralph/INBOX.md proposal
2026-09-25T20:30 | master | DISPATCH: experiment runs the frozen analysis_c1.py now on the completed S1 contrasts (L0−L1 with seeds 42–46, L1−L2) and exports the analysis block; remaining contrasts at Gate B. Pre-registered contrasts computed early cannot influence any later choice; it shortens the end | plan §4 S1 | ralph/results/s1__loss_ladder_v1.json
2026-09-25T20:30 | master | DISPATCH: S4 (T3 oracle decomposition, zero training, plan §4) starts now on CPU at low priority, since training occupies all GPUs; instruction 3 allows it | plan §4 S4, INBOX instruction 3 | auto-research/
2026-09-25T21:00 | writing | Ruling (1) applied: Gate A paragraph keeps the n=5 gate statement (gateA__summary_n5_v1.json) and adds one sentence with pooled n=8 stats from gateA__summary_v1.json (\phm{8}, raw \phm{23.26}, SD \phm{0.41}, window gap \phm{-0.04}); app_s1_arms caption says the L0 row pools eight seeds and the paired contrasts use 42-46. Items (2) literature and (3) S1 contrasts wait on their files | master ruling | writing/section/experiments.tex, writing/tables/spec.json, ralph/phm-spec.json
2026-09-25T20:31 | master | ACCEPT c7b71df (audit + verify-phm re-run by master): Gate A paragraph states the n = 5 gate (0.05 below) and the pooled n = 8 reference (raw 23.26 ± 0.41, gap −0.04, also below) by key; pooling stated in the arm-table caption. Both seed counts agree on the direction; no claim changes | rule 2, rule 10 | writing/section/experiments.tex
2026-09-25T22:10 | writing | S1 interim analysis block bound (analysis_s1, interim=True; contrasts on seeds 42-46). L0−L1 +0.07 dB, verdict removable (TOST). L1−L2 −0.16 dB (detaching k RAISES GT-mean PSNR in 5/5 seeds; Holm CI [−0.28, −0.03] excludes 0, but |Δ| < 0.3 SESOI so rule S-3(b) fails and TOST passes) → verdict removable, not the draft's "inconclusive". Text changed accordingly: loss-ladder paragraph, "L2 differs by two removable steps", k-coupling sentence, intro contribution (2) "removable". s1_contrasts rows L0−L1/L1−L2 and app_s1_arms L0/L1/L2 now measured (L0 row pooled n=8 from gateA__summary_v1.json as ruled; its caption was otherwise false); L2 is bold as best measured arm so far. Provisional L3/L4, S2-arm and S3 paper-style cells re-derived from the measured bases (the §7 27.7 base had put them above every measured arm). verify-phm 85/85, 0 unbacked; audit exit 0; body p4. For master: the only S1 effect with a CI excluding zero favours the DETACHED loss; worth a sentence in the brief | master ruling (3) | writing/section/{experiments,introduction}.tex, writing/tables/*, ralph/phm-spec.json, ralph/PH-LEDGER.md
2026-09-25T20:35 | master | S1 INTERIM (0fbd416; frozen analysis via run_analysis.py, arms enter only with all seeds, Holm padded p = 1 for missing contrasts): L0−L1 (VGG on HVI) +0.07 dB (SD 0.17) → removable; L1−L2 (k detached) −0.16 dB (SD 0.07; CI excludes 0 but |Δ| < 0.3, TOST p = 0.004) → removable by rule S-3/S-4. Interim; verdicts final at Gate B. Oracle passes moved to background on non-training GPUs (they had idled 2 GPUs 80 min) — accepted | plan §4 rules S-3, S-4 | ralph/results/s1__loss_ladder_v1.json analysis.analysis_s1
2026-09-25T20:35 | master | RULING: analysis_c1.py stays frozen byte-for-byte. Its selection_bias() expects oracle labels '<arm>_seed<s>' but the files use 'oracle_<arm>_seed<s>' / 'oracle_gateA_L0_seed<s>': the fix is a label-join adapter in run_analysis.py (rename keys before the call), NOT an edit of the frozen script; run_analysis.py's sha256 is recorded alongside the frozen sha in every analysis block, and the self-test (planted effect / null) is re-run through the wrapper to show identical rule outputs | pre-registration protects the rule, not a label spelling; the wrapper is the documented seam | auto-research/run_analysis.py
2026-09-25T20:35 | master | ACCEPT interim S1 binding (verify-phm 85/85, audit exit 0, tables in sync, partial-ladder wording present): L0−L1 +0.07 removable, L1−L2 −0.16 removable (5/5 seeds favour detached k, CI excludes 0, inside ± 0.3); app_s1_arms L0 at pooled n = 8 by key; provisional cells re-derived from measured bases (still ph) | rule 2, rule 10 | writing/section/experiments.tex
2026-09-25T20:50 | experiment | ralph/results/literature_v1.json: six quoted literature numbers checked against primary sources (all found as printed). Two corrections for writing: (a) GitHub issue #66 was answered by the maintainer (LOLv1 is tested with GT mean; released hyper-parameters not optimal) and closed as completed — SURVEY.md/D_hvi_followups.md "closed without explanation" is wrong; (b) FusionNet's CIDNet 23.50/0.870/0.105 equals the released wo_perc weights without GT mean, so it is most likely not a third-party retraining — "retrainings 23.5-24.0" holds for PAL (23.97) and issue #66 (23.59) only. shafi2026iphoneblur's 0.2-0.4 dB is a 3-seed SD in motion deblurring, not LLIE | master item (2) | ralph/results/literature_v1.json
2026-09-25T20:35 | master | literature_v1.json (5bb14ce): 6 verified entries. FACT corrections for the paper: issue #66 was answered by the maintainer (LOL-v1 tested with GT-mean; hyper-parameters not optimal; closed as completed) — 'closed without explanation' is wrong; FusionNet's 23.50 is the released wo_perc weights evaluated without GT-mean, not a retraining — 'third-party retrainings 23.5–24.0' rests on PAL 23.97 and issue #66 23.59; shafi2026iphoneblur's 0.2–0.4 dB is a 3-seed SD in deblurring. README and issue #66 need bib entries | rule 5: attribution must be exact | ralph/results/literature_v1.json
2026-09-25T22:40 | writing | S1 marked partial until Gate B: loss-ladder paragraph and s1_contrasts caption state two of four contrasts measured, Holm over all four with missing p = 1 (holm_family key). L0-L1/L1-L2 bindings unchanged from 693467a. Recipe and k-coupling sentences shortened to hold p4 | master dispatch | writing/section/experiments.tex, writing/tables/spec.json
2026-09-25T21:10 | experiment | Master ruling applied: analysis_c1.py untouched; run_analysis.py gains a label-join adapter (oracle_<arm>_seed<s> / oracle_gateA_L0_seed<s> -> <arm>_seed<s>) for the frozen selection_bias(), records its own sha256 next to the frozen one in every analysis block, and writes ralph/results/s3__selection_bias_v1.json (arms with complete final reads + oracle entries: L0 at ladder seeds 42-46, L1, L2; status partial). Wrapper self-test (phase0_analysis_selftest.json -> wrapper_selftest): rule outputs identical to the frozen analysis on the planted data, interim padded Holm >= final Holm, adapter maps both label forms — PASS. The self-test also caught a wrapper bug (S2 A0 looked up only under its alias L2), fixed before any S2 data exists | master ruling | auto-research/run_analysis.py
2026-09-25T20:37 | master | ACCEPT 22fe236: oracle label-join adapter in run_analysis.py, analysis_c1.py untouched; wrapper + frozen sha in every analysis block; wrapper self-test PASS (identical rule outputs on planted data; adapter OK); s3__selection_bias_v1.json for L0/L1/L2 (partial); a wrapper alias bug for S2's A0 caught by the self-test before any S2 data | rule 9: the instrument was shown to pass on a planted case through the same seam the paper will use | ralph/results/s3__selection_bias_v1.json
2026-09-25T23:20 | writing | Literature numbers bound as \phm to literature_v1.json with attribution: paper 28.2 (GT-mean, \citep{yan2025hvi}); PAL retraining 24.0 (\citep{li2026pal}); user retraining in issue #66 23.6 (\citep{hvicidnet_issue66}); FusionNet 23.50 stated as the released weights without GT-mean (\citep{shi2025fusionnet, hvicidnet_readme}), no longer called a retraining; shafi 0.2-0.4 stated as a three-seed SD in motion deblurring. Abstract carries the numbers and "reports" but no \citep (style guide: no citations in the abstract). New @misc bib entries hvicidnet_readme (commit eb43d7d) and hvicidnet_issue66, accessed 2026-09-25. Issue #66 "closed without explanation": absent from paper, phase0_related.md and appendix; present only in the author's ralph/related/SURVEY.md line 98, left untouched for the author. S3 folded in: app_s3 now reads s3__selection_bias_v1.json for L0 (seeds 42-46), L1 and L2 (= A0), marked partial in caption and appendix text; the results 0.06 also bound to that file (Gate A paragraph keeps the n=5 keys). Intro trimmed (proxy-ablation sentence) to hold p4. verify-phm 96/96, 0 unbacked; audit exit 0 | master dispatches (literature, S3) | writing/section/*.tex, writing/icml2024/custom.bib, writing/tables/spec.json, ralph/phm-spec.json, ralph/PH-LEDGER.md
2026-09-25T20:38 | master | ACCEPT literature + S3 pass (verify-phm 96/96, audit exit 0, tables in sync, no 'closed without explanation' in paper or related.md): literature numbers by key with attribution, app_s3 on the S3 export (partial) | rules 2, 5 | ralph/phm-spec.json
2026-09-25T20:42 | master | S4 launched (ed0ff82, CPU nice 19): released w_perc on the 40 val images (9 scenes); shifts none / GT-blend 0.1–0.5 / gains 0.5, 2, 4; arms identity, F0 official, F0 identity, G0 mean-match, O-gain and O-all oracles for raw and GT-mean PSNR; ΔE00 split; scene-bootstrap CIs + Wilcoxon; preflight green; ETA ≈ 14–16 h → s4__oracle_decomp_v1.json. Caveat in the JSON: val images are in the released model's training set | plan §4 S4 (R4 design); INBOX instruction 3 | auto-research/s4_oracle_decomp.py
2026-09-25T20:43 | master | CORRECTION (experiment, file-verified): S4 val set = 40 images in 18 scene clusters (manifest n_scenes = 18), not 9; training epoch times unchanged (21.6–22.5 s) with the CPU job running | replaces the 9-scene figure in the previous line | outputs/s4 manifest
2026-09-26T00:56 | master | S1 arm L3 (RGB-only) complete at n = 5; per-seed GT-mean reads 26.28/26.25/26.19/26.06/26.01 vs L2's 26.59–26.87 (experiment's completion lines; the paper cites the analysis keys only). L2−L3 verdict comes from the frozen analysis (rule S-3/S-4) at the daemon's next export; writing binds it under the standing instruction. Remaining S1: L4 × 5 (42 finishing, 43/44 early, 45/46 queued) → Gate B ≈ Sat 17:00 UTC | plan §4 S1 | ralph/RESULTS.md
2026-09-26T01:00 | experiment | FACT (interim, 3 of 4 S1 contrasts): L2-L3 (HVI-space supervision on top of the RGB loss) = +0.577 dB GT-mean PSNR, seed SD 0.168, Holm CI [0.253, 0.901], p_holm 0.006 -> "contributes" by rule S-3; L0-L1 +0.067 (removable), L1-L2 -0.155 (removable). Holm still padded for the missing L2-L4 (conservative); verdicts final only when L4 completes | frozen analysis via run_analysis.py | ralph/results/s1__loss_ladder_v1.json analysis.analysis_s1.decision
2026-09-26T00:56 | master | S1 INTERIM 3/4 (e12d157, frozen rules, Holm padded): L2−L3 HVI-space supervision +0.577 dB GT-mean (SD 0.168; Holm CI [0.253, 0.901]; p_holm 0.006) → CONTRIBUTES (C-S1c); L0−L1 +0.07 removable; L1−L2 −0.16 removable. Final at Gate B when L4 completes (≈ 5 h). S4 at 10/40 images | plan §4 rule S-3 | ralph/results/s1__loss_ladder_v1.json analysis.analysis_s1
2026-09-26T00:57 | writing | S1 L2-L3 bound (interim, 3 of 4): +0.58 dB, contributes, 5/5 seeds, Holm CI [0.25, 0.90] — replaces \ph{0.6} in abstract, intro, results, conclusion; s1_contrasts L2-L3 row and app_s1_arms L3 row now measured; the L0-L1 and L1-L2 Holm CIs narrowed (one fewer padded p) and were regenerated; partial wording 'three of four'. verify-phm 108/108, 0 unbacked; audit exit 0; body p4 | standing instruction | writing/section/*.tex, writing/tables/*, ralph/phm-spec.json, ralph/PH-LEDGER.md
2026-09-26T00:58 | master | ACCEPT S1 3/4 binding (verify-phm 108/108, audit exit 0, tables in sync; 0.58 identical across abstract/intro/results/conclusion) | rule 2, rule 10 | writing/section/*.tex
2026-09-26T05:58 | experiment | A1 (k frozen at 0.2937) moved to its own run s2/rep_ladder_v1_a1 (queue state + ORDER; no job had started). Reason: k_fixed is a protocol field; adding A1 to s2/rep_ladder_v1 (manifest k_fixed = None) would fail the launcher's protocol-equality check, and editing the launcher would change its hash and block every further launch into the existing runs. Same code, same protocol otherwise; the new manifest records k_fixed. The S2 analysis is unaffected (it joins final reads by label); its export is s2__rep_ladder_v1_a1.json | found while checking the first S2 manifest | outputs/queue/queue.json
2026-09-26T05:52 | master | S2 started 05:51 (A2_seed42 on GPU 2; manifest k0 1.1255, scene_v1, arm code sha = S1's). ACCEPT experiment's pre-emption: A1's k_fixed = 0.2937 is a protocol field, so A1's five jobs run as s2/rep_ladder_v1_a1 with their own manifest (plan §2: a protocol change is a new run name); nothing else differs; the analysis joins arms by name so S2 contrasts are unaffected. L4 seed 43 exported; S4 27/40 | plan §2, §4 | outputs/s2/
2026-09-26T06:02 | writing | s2__rep_ladder_v1.json appeared (A0 arm = the five L2 runs; no S2 contrast yet): app_s2_arms A0 row regenerated as measured (26.73 / 23.31, bold as the only measured arm, caption rule), other rows stay \ph; s2_contrasts untouched | standing instruction (table drift) | writing/tables/app_s2_arms.tex, ralph/phm-spec.json
2026-09-26T06:02 | master | ACCEPT app_s2_arms A0 row bound to the S2 export (audit exit 0, verify-phm 0 unbacked on master's run) | rule 2 | writing/tables/app_s2_arms.tex
2026-09-26T07:45 | experiment | FACT — S1 loss ladder COMPLETE (25 runs, final eval15 reads, frozen analysis_c1 via run_analysis.py, Holm across all 4, not interim): L0-L1 (VGG on HVI) +0.067 dB [Holm CI -0.146, 0.280] removable; L1-L2 (k coupling in loss) -0.155 [-0.273, -0.038] removable (CI excludes 0 but |Δ| < 0.3 and TOST p 0.004); L2-L3 (HVI-space supervision) +0.577 [0.253, 0.901], 5/5 seeds, contributes; L2-L4 (C_k-weighted chroma) +0.149 [-0.170, 0.467], TOST p 0.086, inconclusive. GT-mean arm means L0 26.645, L1 26.578, L2 26.734, L3 26.156, L4 26.585 dB. Gate B inputs complete; the verdict call is master's. L3/L4 oracle passes (S3) run when a GPU is free of training | plan §5 Gate B | ralph/results/s1__loss_ladder_v1.json analysis.analysis_s1.decision
2026-09-26T07:42 | master | GATE B: PASS — all 25 S1 runs exported with single final reads; frozen analysis (analysis_c1.py via run_analysis.py, interim = false) holds four contrasts at n = 5. Verdicts (GT-mean PSNR, paired by seed, Holm across 4): L0−L1 VGG-on-HVI +0.07 dB (SD 0.17; CI [−0.15, 0.28]) → REMOVABLE (C-S1a); L1−L2 k coupling −0.16 dB (SD 0.07; CI [−0.27, −0.04], p_holm 0.019; |Δ| < 0.3 SESOI) → REMOVABLE (C-S1b: detaching k helps slightly, below the smallest effect of interest); L2−L3 HVI-space supervision +0.58 dB (SD 0.17; CI [0.25, 0.90], p_holm 0.006) → CONTRIBUTES (C-S1c); L2−L4 C_k-weighted chroma +0.15 dB (SD 0.20; CI [−0.17, 0.47], p_holm 0.36) → INCONCLUSIVE (C-S1d). Arm means (GT-mean): L0 26.65, L1 26.58, L2 26.73, L3 26.16, L4 26.58. Pivot check P-L: L0 vs L2 differ by ≈ +0.09 in L2's favour, not a "difference" → P-L does NOT fire; S2 proceeds under L2 as planned | plan §5 Gate B, §4 rules S-3/S-4, §8 P-L | ralph/results/s1__loss_ladder_v1.json analysis.analysis_s1.decision
2026-09-26T07:42 | master | Two pre-registered quantities are null in the analysis block and must be exported before the paper cites them: mde_db (achieved paired MDE, plan §7) and tost_p per contrast (rule S-4 basis of every "removable"); requested from experiment. S2 continues (A2 seeds 42/43 running) | ruling 3 | ralph/results/s1__loss_ladder_v1.json
2026-09-26T07:44 | writing | GATE B binding: S1 final (interim false). All four contrasts and all five arm rows now \phm; L2-L4 +0.15 dB inconclusive (CI [-0.17, 0.47], TOST fails) bound in results and named in intro contribution (2) and the conclusion list; L1-L2 phrased 'detached k slightly better, 5 of 5, below the 0.3 SESOI'; every 'three of four / Holm padded / partial' phrase removed from the S1 text and caption (S3 table stays marked partial: its export still has only L0-L2). MDE \ph{0.5} and TOST p-values stay \ph pending mde_db/tost_p export. verify-phm 123/123, 0 unbacked; audit exit 0; body p4 | master Gate B dispatch | writing/section/*.tex, writing/tables/*, ralph/phm-spec.json, ralph/PH-LEDGER.md
2026-09-26T07:44 | master | ACCEPT Gate B binding (verify-phm 123/123, audit exit 0 on master's run; partial wording gone from S1; 0.58 headline consistent; MDE/TOST stay ph pending keys) | rule 2, rule 10 | writing/section/*.tex
2026-09-26T07:44 | writing (for master) | analysis_s1.derived now has mde_db (S1 family max 0.34 dB; per contrast 0.28/0.11/0.28/0.34) and tost_p. NOT bound yet: the paper's \ph{0.5} MDE (abstract, intro, method, results x2, conclusion) speaks for both ladders, and S2 has no contrast; binding 0.34 would claim an S2 resolution not measured. Proposal: when S2 lands, bind the MDE as the max over both ladders' family_max (needs one key, e.g. a combined derived.mde_db.all_max), or state per ladder ('0.34 dB for the loss ladder, x for the representation ladder'). TOST p-values have no \ph in the text; nothing to bind. Note: experiment's commit b7feb29 is local-only in the shared checkout; my next push will carry it | derived keys | ralph/results/s1__loss_ladder_v1.json
2026-09-26T07:44 | master | ACCEPT b7feb29: frozen script already exports decision.contrasts.<c>.mde_db (1.66·SD/√5) and tost.{p_lower,p_upper}; run_analysis.py adds analysis.analysis_s1.derived with tost_p = max(p_lower, p_upper) {L0−L1 0.019, L1−L2 0.004, L2−L3 0.990, L2−L4 0.086} and mde_db {0.285, 0.110, 0.279, 0.339}, family_max 0.339 dB, formulas recorded. Achieved MDE 0.34 < plan §7's provisional 0.5. L3/L4 oracle passes chained on CPU after S4 (≈ 10:00) → S3 complete for all S1 arms ≈ 18:00 UTC | ruling 3; plan §7 | ralph/results/s1__loss_ladder_v1.json analysis.analysis_s1.derived
2026-09-26T07:45 | writing | Achieved MDE bound per master: \phm{0.34} (analysis_s1.derived.mde_db.family_max) replaces \ph{0.5} in abstract, intro, method, results (2) and conclusion; the abstract says 'on the loss ladder' and results says 'the loss ladder's achieved MDE' so no S2 resolution is claimed before S2 exists. TOST p bound beside each stated verdict: L0-L1 removable p=0.02, L1-L2 removable p=0.004, L2-L4 inconclusive p=0.09 (derived.tost_p). verify-phm 127/127, 0 unbacked; audit exit 0; body p4 | master dispatch | writing/section/*.tex, ralph/phm-spec.json, ralph/PH-LEDGER.md
2026-09-26T07:48 | master | AUDIT (Gate B draft, auditor 2 of 3): all 39 bound values in experiments/conclusion match; sentence defects queued for writing. OPEN RULE QUESTION: the L2−L3 "contributes" verdict's condition S-3(d) (mixed model agrees in direction) rests on a model with converged = false; experiment asked what the frozen script does on non-convergence and what direction it estimated. Default if the script counts non-convergence as "not contradicting": keep the verdict, state the non-convergence explicitly in the paper, and add a supplementary (non-rule) simpler mixed model as a robustness check | rule S-3(d); transparency over tidiness | ralph/results/s1__loss_ladder_v1.json
2026-09-26T07:48 | master | DISPATCH (pre-registered, plan §3 cross-dataset, zero training): download LOL-v2-Real test, log source + sha256, deduplicate against LOL-v1 train by hash/embedding, evaluate every final S1 checkpoint (and S2 later) once per arm-seed with a labelled script; runs when GPUs free or on CPU; not new scope (plan §3 lists it) | INBOX instruction 3 allows plan-listed zero-training analyses | HVI-PLAN.md §3
2026-09-26T07:49 | master | MILESTONE AUDIT of the Gate B draft (e99ec8e; 3 section auditors + master's tooling): all 127 bound values match their keys; tables in sync; hardware correct. Sentence/caption defects sent to writing (14 items): Gate A paragraph must state the window check failed by 0.05 (P-A) while the SD criterion passed; extra A2/A3 seeds are queued, not run; the 445-vs-485 attribution is untested and is softened; effect directions for L0−L1 and L2−L4; L2−L3 must cite conditions a–c and the mixed-model non-convergence (edit held for experiment's facts); S3 "per arm" is partial until 18:00; seed SD not summed with mean shifts; "slightly better"/"survives the seeded test" → vocabulary; PAL/#66 protocol hedge; "so the MDE" → neutral; "answer for every step" overclaim; captions (s2 tables provisional, app_s1 "pooled n = 8", app_s3 rows as oracles land); provisional s2_contrasts cells self-consistent; FP32 duplicate; bind k init and loss weights (keys exist). Provisional S2/S4/LOL-v2 sentences stay as-if-final by convention until their JSONs exist | persona §7 | ralph/DECISIONS.md
2026-09-26T07:49 | master | RULING on S-3(d) for L2−L3: the frozen rule (analysis_c1.py:178) checks the mixed-model coefficient's sign, not the convergence flag; the non-convergence is the seed variance at the zero boundary (vc.seed 0.0049, vc.image 7.77, coef 0.577, p 1.4e-15); the supplementary image-only random-intercept model converges with coef 0.577 (p 4.7e-15) and every contrast keeps sign and estimate under both supplementary fits (349706e). VERDICT STANDS: contributes. The paper states the boundary non-convergence and cites the supplementary model by key as a robustness check, labelled non-rule | transparency; the rule as frozen was met | ralph/results/s1__loss_ladder_v1.json decision.contrasts.L2-L3.mixed_model, derived.mixed_supplementary
2026-09-26T07:51 | writing | Master's 14-item audit of e99ec8e + resolved item (5) applied in one pass: Gate A says the raw mean MISSED the window by \phm{-0.05} (P-A) while the separate seed-SD criterion passed; seeds 47-49 added to A0, will be added to A2/A3; shortfall 'coincides with' three protocol differences, training-set reduction not isolated; every contrast states its direction (VGG removal lowers 0.07, detaching raises, C_k weighting lowers 0.15); L2-L3 cites Holm CI [0.25, 0.90], mean > 2 SD (0.17), 5/5 signs and the mixed model (coef 0.58), with master's exact convergence wording in new appendix subsection sec:app-mixed (seed vc 0.005, image-only coef 0.58, rule tests sign not convergence) and a pointer in the body (page budget); 'slightly better' and 'survives the seeded test' removed; S3 sentence says partial (L0-L2), seed SD stated separately from mean shifts; intro hedges PAL (GT-mean status not stated) and issue #66 (presumably without); abstract MDE link neutral; 'answer for every step' removed; captions: s2 tables provisional, app_s1 'pooled n = 8', app_s3 gains L3/L4 rows (ph until oracle keys); s2_contrasts ph cells made consistent with their ph verdicts; duplicate FP32 removed; k init and loss weights 0.5/50/0.01 bound to phase0_codebase keys. LOL-v2 transfer paragraph moved to the cross-dataset appendix subsection to hold p4. verify-phm 137/137, 0 unbacked; audit exit 0 | master audit | writing/section/*.tex, writing/tables/*, ralph/phm-spec.json, ralph/PH-LEDGER.md
2026-09-26T07:51 | master | ACCEPT the 14-item + mixed-model pass (verify-phm 137/137, audit exit 0 on master's run; window-miss stated; tense fixed; vocabulary clean; s2 tables provisional; pooled n = 8; convergence stated in appendix; refs on p5) | persona §7 | writing/section/*.tex
2026-09-26T09:05 | experiment | FACT: LOL-v2-Real test (HF mirror okhater/lolv2-real @db297910, 100 pairs, all file sha256 logged) — 91/100 GTs are PIXEL-IDENTICAL to LOL-v1 our485 GTs (exact, pHash and DINOv2 rules all flag the same 91); 9 pairs survive dedup against LOL-v1 train. Evaluation HELD: the 9 survivors may be copies of LOL-v1 eval15 (the test split); checking needs eval15 hashes, which only whitelisted scripts may read — asked master for a ruling before any LOL-v2 read | plan §3 cross-dataset check | ralph/results/lolv2real_v1.json
2026-09-26T07:54 | master | FACT (1e3bee3): LOL-v2-Real test (HF mirror okhater/lolv2-real @db297910, sha256 logged; no official scriptable source) — 91/100 GTs are pixel-identical to LOL-v1 our485 GTs (exact, pHash, DINOv2 agree); the remaining 9 may duplicate eval15 | experiment's dedup | ralph/results/lolv2real_v1.json
2026-09-26T07:54 | master | RULING: (a) lolv2real.py MAY hash eval15 GT pixels for deduplication only — no metric, no model output — under a recorded permit exactly like scene_split.py's eval15 exclusion (a listing-level read, not a test-split evaluation); (b) if ≥ 5 non-duplicate images survive, evaluate every final checkpoint on them once, labelled lolv2_dedup and reported as DESCRIPTIVE with n and scene count stated, no verdict; if < 5 survive, evaluate nothing; (c) either way the paper reports the duplication fact as the cross-dataset result: "LOL-v2-Real test duplicates ≥ 91 % of LOL-v1 training GTs, so it cannot serve as a generalisation test for LOL-v1-trained models" (key-bound counts) | plan §3 lists the check as secondary; a dataset fact is a finding; integrity rule 4 concerns evaluation, and the hash is not one | ralph/results/lolv2real_v1.json
2026-09-26T09:20 | experiment | Master ruling applied (LOL-v2-Real): eval15 GT pixel hashes (sha256 + pHash; no metric, model output or embedding) under a runtime-registered permit recorded in lolv2real_v1.json (hvilib.py is hash-pinned by the run manifests, so its whitelist was not edited). Counts: n_test 100, n_dup_train exact/phash/dino 91/91/91, n_dup_eval15 0, n_kept 9, n_scenes_kept 3 -> >= 5, so every S1 final checkpoint is read once on the 9 (labels lolv2_<arm>_seed<s>, kind descriptive), CPU nice 19 | master ruling | ralph/results/lolv2real_v1.json counts
2026-09-26T07:55 | master | ACCEPT 8bf0f09: LOL-v2-Real dedup keys (n_test 100, n_dup_train_any 91, n_dup_eval15 0, n_kept 9, n_scenes_kept 3); eval15 GT hashed under a recorded runtime permit (hvilib.py untouched, its hash pinned); 25 descriptive S1 reads on the 9 images (labels lolv2_<arm>_seed<s>, n = 9, 3 scenes) on CPU | ruling of 08:0x; descriptive only, no verdict | ralph/results/lolv2real_v1.json
2026-09-26T08:01 | writing | LOL-v2-Real rewritten as the cross-dataset result (appendix sec:app-cross): of \phm{100} test pairs \phm{91} have a GT pixel-identical to a LOL-v1 training GT (same 91 by pHash and DINOv2), \phm{0} match eval15, \phm{9} images from \phm{3} scenes remain (lolv2real_v1.json counts.*); the set cannot test generalisation for LOL-v1-trained models, no cross-dataset claim is made; provisional '4 of 5 seeds' and '22.0' dropped. Since 9 >= 5, the text says each loss-arm final checkpoint is scored once as a descriptive read with no verdict; the read values are NOT quoted yet (2 of the S1 checkpoints scored so far, no aggregate key) — will quote with n and scene count once all land. Body references (method scope, results recipe) updated to match | master standing instruction | writing/section/{appendix,method,experiments}.tex, ralph/phm-spec.json, ralph/PH-LEDGER.md
